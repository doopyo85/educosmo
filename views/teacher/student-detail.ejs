<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>학생 상세 정보</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px 0;
            overflow-y: auto;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
        }
        .container {
            max-width: 900px;
            background: white;
            border-radius: 10px;
            padding: 40px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
            margin: 20px auto;
        }
        h2 {
            text-align: center;
            margin-bottom: 30px;
            color: #333;
        }
        .form-section {
            margin-bottom: 30px;
        }
        .form-section h4 {
            border-bottom: 2px solid #667eea;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .table-container {
            max-height: 300px;
            overflow-y: auto;
        }
        .submission-card {
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 10px;
            transition: all 0.3s;
        }
        .submission-card:hover {
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
    </style>
</head>
<body>
    <div class="container">
        <h2><i class="bi bi-person-circle me-2"></i>학생 상세 정보</h2>

        <form id="studentDetailForm">
            <div class="form-section">
                <h4>기본 정보</h4>
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label">이름</label>
                        <input type="text" class="form-control" name="name" value="<%= student.name %>">
                    </div>
                    <div class="col-md-6 mb-3">
                        <label class="form-label">아이디</label>
                        <input type="text" class="form-control" name="userID" value="<%= student.userID %>" readonly>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label class="form-label">이메일</label>
                        <input type="email" class="form-control" name="email" value="<%= student.email %>">
                    </div>
                    <div class="col-md-6 mb-3">
                        <label class="form-label">전화번호</label>
                        <input type="text" class="form-control" name="phone" value="<%= student.phone %>">
                    </div>
                    <div class="col-md-12 mb-3">
                        <label class="form-label">비밀번호 변경 <small class="text-muted">(변경하지 않으려면 비워두세요)</small></label>
                        <input type="password" class="form-control" name="password" placeholder="새 비밀번호">
                    </div>
                </div>
            </div>

            <div class="d-flex justify-content-between align-items-center mb-4">
                <button type="submit" class="btn btn-success">
                    <i class="bi bi-check-lg me-1"></i>저장
                </button>
                <button type="button" class="btn btn-secondary" onclick="window.close()">
                    <i class="bi bi-x-lg me-1"></i>닫기
                </button>
            </div>
        </form>

        <hr class="my-4">

        <!-- 🔥 제출물 탭 -->
        <div class="form-section">
            <h4><i class="bi bi-folder-check me-2"></i>제출물 (Entry/Scratch)</h4>
            
            <ul class="nav nav-tabs mb-3" id="submissionTabs" role="tablist">
                <li class="nav-item" role="presentation">
                    <button class="nav-link active" id="entry-tab" data-bs-toggle="tab" data-bs-target="#entry-submissions" type="button">
                        <i class="bi bi-file-earmark-code"></i> Entry
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="scratch-tab" data-bs-toggle="tab" data-bs-target="#scratch-submissions" type="button">
                        <i class="bi bi-box"></i> Scratch
                    </button>
                </li>
            </ul>
            
            <div class="tab-content" id="submissionTabContent">
                <div class="tab-pane fade show active" id="entry-submissions" role="tabpanel">
                    <div id="entry-list"></div>
                </div>
                <div class="tab-pane fade" id="scratch-submissions" role="tabpanel">
                    <div id="scratch-list"></div>
                </div>
            </div>
        </div>

        <hr class="my-4">

        <div class="form-section">
            <h4><i class="bi bi-clock-history me-2"></i>학습 기록</h4>
            <div class="table-container">
                <table class="table table-hover">
                    <thead class="table-light">
                        <tr>
                            <th>일시</th>
                            <th>활동</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (logs && logs.length > 0) { %>
                            <% logs.forEach(log => { %>
                                <tr>
                                    <td><%= new Date(log.start_time).toLocaleString('ko-KR') %></td>
                                    <td><%= log.content_name %></td>
                                </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="2" class="text-center text-muted">학습 기록이 없습니다.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>

        <hr class="my-4">

        <div class="form-section">
            <h4><i class="bi bi-box-arrow-in-right me-2"></i>로그인 기록</h4>
            <div class="table-container">
                <table class="table table-hover">
                    <thead class="table-light">
                        <tr>
                            <th>일시</th>
                            <th>상태</th>
                            <th>접속 IP</th>
                            <th>브라우저</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (activityLogs && activityLogs.length > 0) { %>
                            <% activityLogs.forEach(log => { %>
                                <tr>
                                    <td><%= new Date(log.created_at).toLocaleString('ko-KR') %></td>
                                    <td>
                                        <span class="badge <%= log.status === 'login' ? 'bg-success' : log.status === 'logout' ? 'bg-secondary' : 'bg-light text-dark' %>">
                                            <%= log.status === 'login' ? '로그인' : log.status === 'logout' ? '로그아웃' : '활동중' %>
                                        </span>
                                    </td>
                                    <td><%= log.ip_address %></td>
                                    <td><%= log.user_agent ? log.user_agent.substring(0, 50) + '...' : '-' %></td>
                                </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="4" class="text-center text-muted">로그인 기록이 없습니다.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const studentId = <%= student.id %>;
        
        // 학생 정보 수정
        document.getElementById('studentDetailForm').addEventListener('submit', async (e) => {
            e.preventDefault();

            const formData = {
                name: e.target.name.value,
                email: e.target.email.value,
                phone: e.target.phone.value,
                password: e.target.password.value
            };

            try {
                const isMyProfile = <%= locals.isMyProfile || false %>;
                const url = isMyProfile ? '/api/my-profile' : `/teacher/api/students/${studentId}`;
                
                const response = await fetch(url, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(formData)
                });

                const result = await response.json();
                if (result.success) {
                    alert('수정이 완료되었습니다.');
                    location.reload();
                } else {
                    alert('수정 실패: ' + result.message);
                }
            } catch (err) {
                console.error(err);
                alert('오류 발생');
            }
        });
        
        // 🔥 제출물 로드
        async function loadSubmissions(platform) {
            const listId = `${platform}-list`;
            const container = document.getElementById(listId);
            
            container.innerHTML = '<div class="text-center py-3"><div class="spinner-border spinner-border-sm"></div> 로딩 중...</div>';
            
            try {
                const response = await fetch(`/teacher/api/students/${studentId}/submissions?platform=${platform}`);
                const result = await response.json();
                
                if (result.success && result.submissions.length > 0) {
                    container.innerHTML = result.submissions.map(sub => `
                        <div class="submission-card">
                            <div class="d-flex justify-content-between align-items-start mb-2">
                                <div>
                                    <h6 class="mb-1">
                                        <i class="bi bi-file-earmark-code-fill text-primary"></i>
                                        ${sub.project_name}
                                    </h6>
                                    <small class="text-muted">
                                        <i class="bi bi-calendar"></i> ${new Date(sub.submitted_at).toLocaleString('ko-KR')}
                                        &nbsp;&nbsp;
                                        <i class="bi bi-hdd"></i> ${sub.file_size_mb} MB
                                    </small>
                                </div>
                                <div class="btn-group btn-group-sm">
                                    <button class="btn btn-outline-primary" onclick="viewSubmission('${sub.s3_url}', '${platform}')">
                                        <i class="bi bi-eye"></i> 보기
                                    </button>
                                    <button class="btn btn-outline-success" onclick="downloadSubmission('${sub.s3_url}', '${sub.project_name}')">
                                        <i class="bi bi-download"></i> 다운로드
                                    </button>
                                </div>
                            </div>
                        </div>
                    `).join('');
                } else {
                    container.innerHTML = `
                        <div class="text-center text-muted py-4">
                            <i class="bi bi-inbox" style="font-size: 48px; opacity: 0.3;"></i>
                            <p class="mt-2">제출한 작품이 없습니다.</p>
                        </div>
                    `;
                }
            } catch (error) {
                console.error('제출물 로드 실패:', error);
                container.innerHTML = '<div class="alert alert-danger">제출물을 불러올 수 없습니다.</div>';
            }
        }
        
        // 제출물 보기
        function viewSubmission(s3Url, platform) {
            const workspaceUrl = platform === 'entry' 
                ? `/entry/?s3Url=${encodeURIComponent(s3Url)}`
                : `/scratch/?s3Url=${encodeURIComponent(s3Url)}`;
            window.open(workspaceUrl, '_blank');
        }
        
        // 제출물 다운로드
        function downloadSubmission(s3Url, fileName) {
            const a = document.createElement('a');
            a.href = s3Url;
            a.download = fileName;
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
        }
        
        // 페이지 로드 시 Entry 제출물 로드
        document.addEventListener('DOMContentLoaded', function() {
            loadSubmissions('entry');
        });
        
        // 탭 전환 이벤트
        document.getElementById('entry-tab').addEventListener('shown.bs.tab', function() {
            loadSubmissions('entry');
        });
        
        document.getElementById('scratch-tab').addEventListener('shown.bs.tab', function() {
            loadSubmissions('scratch');
        });
    </script>
</body>
</html>
