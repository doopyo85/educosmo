<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>학생 상세 정보</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <style>
        :root {
            --bg-color: #FFFFFF;
            --text-primary: #1D1D1F;
            --text-secondary: #86868B;
            --accent-color: #0071E3;
            --danger-color: #FF3B30;
            --input-bg: #F5F5F7;
            --input-radius: 12px;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-primary);
            font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            margin: 0;
            padding: 16px;
            box-sizing: border-box;
            min-height: 100vh;
        }

        .modal-container {
            width: 100%;
            max-width: 100%;
            padding: 0;
            position: relative;
        }

        h2 {
            font-size: 20px;
            font-weight: 700;
            text-align: center;
            margin-bottom: 20px;
            color: var(--text-primary);
        }

        .form-group {
            margin-bottom: 12px;
        }

        .form-label {
            font-size: 12px;
            font-weight: 500;
            color: var(--text-secondary);
            margin-bottom: 4px;
            display: block;
        }

        .form-control {
            background-color: var(--input-bg);
            border: none;
            border-radius: 8px;
            padding: 8px 12px;
            font-size: 14px;
            color: var(--text-primary);
            width: 100%;
            transition: all 0.2s ease;
        }

        .form-control:focus {
            background-color: #fff;
            box-shadow: 0 0 0 2px var(--accent-color) inset;
            outline: none;
        }

        .form-control[readonly] {
            background-color: #F0F0F2;
            color: var(--text-secondary);
            cursor: not-allowed;
        }

        .row-compact {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        .btn-group-custom {
            display: flex;
            gap: 10px;
            margin-top: 20px;
        }

        .btn-custom {
            flex: 1;
            padding: 10px;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            border: none;
            cursor: pointer;
            transition: transform 0.1s;
        }

        .btn-save {
            background-color: var(--accent-color);
            color: white;
        }

        .btn-cancel {
            background-color: #E5E5EA;
            color: var(--text-primary);
        }

        .btn-custom:active {
            transform: scale(0.98);
        }

        .btn-close-custom {
            position: absolute;
            top: -10px;
            right: 0;
            background: none;
            border: none;
            font-size: 20px;
            color: var(--text-secondary);
            cursor: pointer;
            padding: 5px;
            transition: color 0.2s;
        }

        .btn-close-custom:hover {
            color: var(--text-primary);
        }
    </style>
</head>

<body>
    <div class="modal-container">
        <button type="button" class="btn-close-custom" onclick="window.close()">
            <i class="bi bi-x-lg"></i>
        </button>
        <h2>학생 상세 정보</h2>

        <form id="studentDetailForm">
            <div class="form-group">
                <label class="form-label">소속 센터</label>
                <input type="text" class="form-control" value="<%= student.centerName || '소속 없음' %>" readonly>
            </div>

            <div class="row-compact">
                <div class="form-group">
                    <label class="form-label">이름</label>
                    <input type="text" class="form-control" name="name" value="<%= student.name %>">
                </div>
                <div class="form-group">
                    <label class="form-label">아이디</label>
                    <input type="text" class="form-control" name="userID" value="<%= student.userID %>" readonly>
                </div>
            </div>

            <div class="form-group">
                <label class="form-label">이메일</label>
                <input type="email" class="form-control" name="email" value="<%= student.email %>">
            </div>

            <div class="form-group">
                <label class="form-label">전화번호</label>
                <input type="text" class="form-control" name="phone" value="<%= student.phone %>">
            </div>

            <div class="form-group">
                <label class="form-label">비밀번호 변경 <small style="color: var(--text-secondary); font-weight: 400;">(변경 시에만
                        입력)</small></label>
                <input type="password" class="form-control" name="password" placeholder="••••••••">
            </div>

            <div class="btn-group-custom">
                <button type="button" class="btn-custom btn-cancel" onclick="window.close()">닫기</button>
                <button type="submit" class="btn-custom btn-save">저장</button>
            </div>
        </form>
    </div>

    <script>
        const studentId = <%= student.id %>;

        document.getElementById('studentDetailForm').addEventListener('submit', async (e) => {
            e.preventDefault();

            const formData = {
                name: e.target.name.value,
                email: e.target.email.value,
                phone: e.target.phone.value,
                password: e.target.password.value
            };

            try {
                const isMyProfile = <%= locals.isMyProfile || false %>;
                const url = isMyProfile ? '/api/my-profile' : `/teacher/api/students/${studentId}`;

                const response = await fetch(url, {
                    method: 'PUT',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });

                const result = await response.json();
                if (result.success) {
                    alert('수정이 완료되었습니다.');
                    location.reload();
                } else {
                    alert('수정 실패: ' + result.message);
                }
            } catch (err) {
                console.error(err);
                alert('오류 발생');
            }
        });
    </script>
</body>

</html>