<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>내 파일 관리 - 코딩앤플레이</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .platform-tabs {
            margin-bottom: 20px;
            border-bottom: 2px solid #e0e0e0;
        }
        .platform-tab {
            padding: 10px 20px;
            border: none;
            background: none;
            color: #666;
            cursor: pointer;
            font-weight: 500;
            transition: all 0.3s;
        }
        .platform-tab:hover {
            color: #007bff;
        }
        .platform-tab.active {
            color: #007bff;
            border-bottom: 3px solid #007bff;
        }
        .s3-browser-container {
            background: white;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            padding: 20px;
            min-height: 600px;
        }
        .page-header {
            margin-bottom: 30px;
            padding-bottom: 15px;
            border-bottom: 2px solid #f0f0f0;
        }
        .page-header h2 {
            color: #333;
            font-weight: 600;
        }
        .info-card {
            background: #f8f9fa;
            border-left: 4px solid #007bff;
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 4px;
        }
        .info-card .icon {
            color: #007bff;
            font-size: 1.5rem;
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>
    
    <div class="container mt-4">
        <!-- 페이지 헤더 -->
        <div class="page-header">
            <h2>
                <i class="bi bi-folder2-open me-2"></i>
                내 파일 관리
            </h2>
            <p class="text-muted mb-0">내가 만든 프로젝트와 작품을 관리하세요</p>
        </div>

        <!-- 안내 카드 -->
        <div class="info-card">
            <div class="d-flex align-items-center">
                <i class="bi bi-info-circle icon"></i>
                <div>
                    <strong>파일 관리 안내</strong>
                    <p class="mb-0 small">플랫폼별로 내 작품을 확인하고 관리할 수 있습니다. 파일을 업로드하거나 삭제할 수 있습니다.</p>
                </div>
            </div>
        </div>

        <!-- 플랫폼 탭 -->
        <div class="platform-tabs">
            <button class="platform-tab active" data-platform="all">
                <i class="bi bi-grid-3x3-gap me-1"></i>
                전체 보기
            </button>
            <button class="platform-tab" data-platform="scratch">
                <i class="bi bi-puzzle me-1"></i>
                Scratch
            </button>
            <button class="platform-tab" data-platform="entry">
                <i class="bi bi-code-square me-1"></i>
                Entry
            </button>
            <button class="platform-tab" data-platform="gallery">
                <i class="bi bi-image me-1"></i>
                Gallery
            </button>
            <button class="platform-tab" data-platform="portfolio">
                <i class="bi bi-briefcase me-1"></i>
                Portfolio
            </button>
        </div>

        <!-- S3 브라우저 컨테이너 -->
        <div class="s3-browser-container">
            <div id="s3-browser"></div>
        </div>
    </div>

    <%- include('../partials/footer') %>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/components/S3Explorer.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // S3Explorer 초기화
            window.s3Explorer = new S3Explorer({
                containerId: 's3-browser',
                scope: 'self',
                rootPath: 'users/<%= userID %>/',
                userID: '<%= userID %>',
                userRole: 'student',
                enableUpload: true,
                enableDelete: true,
                enablePreview: true
            });

            // 플랫폼 탭 전환 이벤트
            document.querySelectorAll('.platform-tab').forEach(tab => {
                tab.addEventListener('click', function() {
                    // 활성 탭 변경
                    document.querySelectorAll('.platform-tab').forEach(t => t.classList.remove('active'));
                    this.classList.add('active');

                    // 플랫폼별 경로로 이동
                    const platform = this.dataset.platform;
                    switchPlatform(platform);
                });
            });

            // 플랫폼 전환 함수
            function switchPlatform(platform) {
                if (platform === 'all') {
                    window.s3Explorer.navigateTo('users/<%= userID %>/');
                } else {
                    window.s3Explorer.navigateTo('users/<%= userID %>/' + platform + '/');
                }
            }
        });
    </script>
</body>
</html>
