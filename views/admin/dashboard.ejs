<!DOCTYPE html>
<html lang="ko">

<head>
    <%- include('../partials/header') %>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
        <!-- Board Layout Style (Teacher Style) -->
        <link rel="stylesheet" href="/css/board-layout.css">
        <link rel="stylesheet" href="/css/dashboard.css">
        <style>
            /* =========================================
           Apple UI Style & Layout Overrides
           ========================================= */
            body {
                background-color: #f5f5f7;
                /* Apple Light Gray */
                font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
                color: #1d1d1f;
            }

            /* 1. Layout Overrides (Full Stretch) */
            .board-layout-container {
                max-width: none !important;
                /* Remove constraints */
                width: 100%;
                margin: 0 !important;
                padding: 20px 40px;
                /* Outer spacing */
                gap: 30px;
            }

            .board-sidebar {
                width: 260px;
                min-width: 260px;
                position: sticky;
                top: 20px;
                height: calc(100vh - 40px);
                border: none;
                box-shadow: none;
                background: transparent;
            }

            /* Sidebar Styling (Apple Style) */
            .board-sidebar-header {
                background: transparent;
                color: #86868b;
                font-size: 0.8rem;
                text-transform: uppercase;
                letter-spacing: 0.05em;
                padding: 10px 15px;
                font-weight: 600;
            }

            .board-sidebar .board-nav-group {
                margin-bottom: 24px;
            }

            .board-category-item {
                border-radius: 12px !important;
                margin-bottom: 4px;
                padding: 10px 16px;
                color: #424245;
                font-weight: 500;
                font-size: 0.95rem;
                transition: all 0.2s ease;
                border: none !important;
            }

            .board-category-item:hover {
                background-color: rgba(0, 0, 0, 0.04);
                color: #1d1d1f;
            }

            .board-category-item.active {
                background-color: #0071e3 !important;
                /* Apple Blue */
                color: white !important;
                box-shadow: 0 4px 12px rgba(0, 113, 227, 0.3);
            }

            .board-category-item.text-dark.fw-bold {
                padding-left: 10px;
                font-size: 1.1rem;
                margin-bottom: 15px;
                color: #1d1d1f !important;
            }

            .board-main-content {
                flex: 1;
                max-width: none !important;
                border: none;
                box-shadow: none;
                background: transparent;
                padding: 0;
            }

            /* 2. Content Sections (Apple Card Style) */
            .page-header-bar {
                margin-bottom: 24px;
                padding-bottom: 0;
                border-bottom: none !important;
            }

            .section-header-title {
                font-size: 28px;
                font-weight: 700;
                letter-spacing: -0.01em;
            }

            /* Stats Cards */
            .stat-card {
                background: white;
                border-radius: 20px;
                padding: 24px;
                box-shadow: 0 4px 24px rgba(0, 0, 0, 0.04);
                border: 1px solid rgba(0, 0, 0, 0.02);
                height: 100%;
                transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
                position: relative;
                overflow: hidden;
            }

            .stat-card:hover {
                transform: translateY(-4px) scale(1.01);
                box-shadow: 0 12px 32px rgba(0, 0, 0, 0.08);
            }

            .stat-card .card-label {
                color: #86868b;
                font-size: 0.9rem;
                font-weight: 500;
                margin-bottom: 8px;
                display: flex;
                align-items: center;
                gap: 6px;
            }

            .stat-card .card-value {
                font-size: 36px;
                font-weight: 700;
                color: #1d1d1f;
                letter-spacing: -0.02em;
                line-height: 1.1;
            }

            /* Colorful Accents for Cards */
            .stat-card.accent-blue::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 6px;
                height: 100%;
                background: #0071e3;
            }

            .stat-card.accent-green::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 6px;
                height: 100%;
                background: #34c759;
            }

            .stat-card.accent-purple::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 6px;
                height: 100%;
                background: #5856d6;
            }

            .stat-card.accent-orange::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 6px;
                height: 100%;
                background: #ff9500;
            }

            /* Chart Cards */
            .chart-card {
                background: white;
                border-radius: 20px;
                padding: 24px;
                box-shadow: 0 4px 24px rgba(0, 0, 0, 0.04);
                border: 1px solid rgba(0, 0, 0, 0.02);
                margin-bottom: 24px;
            }

            .chart-card h5 {
                font-size: 1.1rem;
                font-weight: 600;
                margin-bottom: 20px;
                color: #1d1d1f;
            }

            /* Tables */
            .table-container {
                background: white;
                border-radius: 20px;
                box-shadow: 0 4px 24px rgba(0, 0, 0, 0.04);
                overflow: hidden;
                padding: 20px;
            }

            .table thead th {
                font-size: 0.8rem;
                text-transform: uppercase;
                color: #86868b;
                font-weight: 600;
                border-bottom: 1px solid #e5e5ea;
                padding: 12px 16px;
            }

            .table tbody td {
                font-size: 0.95rem;
                padding: 16px;
                vertical-align: middle;
                color: #1d1d1f;
                border-bottom: 1px solid #f5f5f7;
            }

            .table-hover tbody tr:hover {
                background-color: #f5f5f7;
            }
        </style>
</head>

<body>
    <div class="board-layout-container">
        <!-- 🔥 왼쪽 네비게이션 (Fixed Sidebar) -->
        <div class="board-sidebar">
            <div class="mb-4 px-3 d-flex align-items-center gap-2">
                <i class="bi bi-shield-lock fs-4 text-dark"></i>
                <span class="fw-bold fs-5 text-dark">Admin</span>
            </div>

            <!-- 대시보드 그룹 -->
            <div class="board-nav-group">
                <div class="board-sidebar-header">Monitoring</div>
                <div class="d-flex flex-column gap-1">
                    <a class="board-category-item nav-link active" href="#" data-section="overview">
                        <i class="bi bi-speedometer2 me-2"></i> 대시보드 개요
                    </a>
                    <a class="board-category-item nav-link" href="#" data-section="learning">
                        <i class="bi bi-bar-chart-line me-2"></i> 학습 현황
                    </a>
                    <a class="board-category-item nav-link" href="#" data-section="centers">
                        <i class="bi bi-building me-2"></i> 센터별 통계
                    </a>
                </div>
            </div>

            <!-- 관리 그룹 -->
            <div class="board-nav-group">
                <div class="board-sidebar-header">System</div>
                <div class="d-flex flex-column gap-1">
                    <a class="board-category-item nav-link" href="#" data-section="users">
                        <i class="bi bi-people me-2"></i> 사용자 관리
                    </a>
                    <a class="board-category-item nav-link" href="#" data-section="permissions">
                        <i class="bi bi-shield-lock me-2"></i> 권한 설정
                    </a>
                    <a class="board-category-item nav-link" href="#" data-section="sql-manager">
                        <i class="bi bi-database me-2"></i> SQL 관리
                    </a>
                    <a class="board-category-item external-link" href="/s3/browser">
                        <i class="bi bi-cloud me-2"></i> S3 파일 관리
                    </a>
                </div>
            </div>
        </div>

        <!-- 🔥 메인 콘텐츠 (Stretched) -->
        <div class="board-main-content">
            <!-- 개요 섹션 -->
            <div id="overview-section" class="section" style="display: block;">
                <!-- Header -->
                <div class="page-header-bar d-flex justify-content-between align-items-end">
                    <div>
                        <small class="text-muted fw-bold">Overview</small>
                        <h1 class="section-header-title mb-0">대시보드</h1>
                    </div>
                    <button type="button" class="btn btn-outline-secondary btn-sm rounded-pill px-3"
                        onclick="refreshStats()">
                        <i class="bi bi-arrow-clockwise me-1"></i> 업데이트
                    </button>
                </div>

                <!-- 통계 카드 (Apple Style) -->
                <!-- Data Mapping: totalUsers, studentCount, teacherCount activeCenters (Available in correct API) -->
                <div class="row g-4 mb-5">
                    <div class="col-md-3">
                        <div class="stat-card accent-blue">
                            <div class="card-label"><i class="bi bi-people-fill"></i> 전체 사용자</div>
                            <div class="card-value" id="totalUsers">-</div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="stat-card accent-green">
                            <div class="card-label"><i class="bi bi-backpack-fill"></i> 학생 수</div>
                            <!-- studentCount로 수정 (기존 totalActivities는 데이터 없음) -->
                            <div class="card-value" id="studentCount">-</div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="stat-card accent-purple">
                            <div class="card-label"><i class="bi bi-person-video3"></i> 강사 수</div>
                            <!-- teacherCount로 수정 (기존 avgDailyActivities는 데이터 없음) -->
                            <div class="card-value" id="teacherCount">-</div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="stat-card accent-orange">
                            <div class="card-label"><i class="bi bi-geo-alt-fill"></i> 활성 센터</div>
                            <div class="card-value" id="activeCenters">-</div>
                        </div>
                    </div>
                </div>

                <!-- 차트 영역 -->
                <div class="row g-4">
                    <div class="col-md-8">
                        <div class="chart-card h-100">
                            <h5>일별 활동량</h5>
                            <div class="chart-container" style="position: relative; height: 300px;">
                                <canvas id="dailyActivityChart"></canvas>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="chart-card h-100">
                            <h5>메뉴별 접속 통계</h5>
                            <div class="chart-container" style="position: relative; height: 300px;">
                                <canvas id="menuAccessChart"></canvas>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 사용자 관리 섹션 -->
            <div id="users-section" class="section" style="display: none;">
                <div class="page-header-bar">
                    <small class="text-muted fw-bold">System</small>
                    <h1 class="section-header-title">사용자 관리</h1>
                </div>
                <div class="table-container">
                    <div class="table-responsive">
                        <table id="usersTable" class="table table-hover mb-0">
                            <thead>
                                <tr>
                                    <th data-sort="no">NO</th>
                                    <th data-sort="userID">ID</th>
                                    <th data-sort="name">이름</th>
                                    <th data-sort="role">역할</th>
                                    <th data-sort="centerID">센터</th>
                                    <th data-sort="last_activity">최근활동</th>
                                    <th data-sort="activity_count">활동수</th>
                                    <th data-sort="created_at">가입일</th>
                                    <th class="text-end">작업</th>
                                </tr>
                                <tr>
                                    <th><input type="text" class="form-control form-control-sm column-filter"
                                            data-field="no"></th>
                                    <th><input type="text" class="form-control form-control-sm column-filter"
                                            data-field="userID" placeholder="ID검색"></th>
                                    <th><input type="text" class="form-control form-control-sm column-filter"
                                            data-field="name" placeholder="이름검색"></th>
                                    <th>
                                        <select class="form-select form-select-sm column-filter" data-field="role">
                                            <option value="">전체</option>
                                            <option value="student">학생</option>
                                            <option value="teacher">강사</option>
                                            <option value="manager">매니저</option>
                                        </select>
                                    </th>
                                    <th><input type="text" class="form-control form-control-sm column-filter"
                                            data-field="centerID"></th>
                                    <th></th>
                                    <th></th>
                                    <th></th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody id="usersTableBody"></tbody>
                        </table>
                    </div>
                </div>
            </div>

            <!-- 학습 현황 섹션 -->
            <div id="learning-section" class="section" style="display: none;">
                <div class="page-header-bar">
                    <small class="text-muted fw-bold">Monitoring</small>
                    <h1 class="section-header-title">학습 현황</h1>
                </div>
                <div class="row g-4">
                    <div class="col-md-6">
                        <div class="chart-card h-100">
                            <h5>콘텐츠별 학습 현황</h5>
                            <canvas id="learningContentChart"></canvas>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="chart-card h-100">
                            <h5>시간대별 학습 분포</h5>
                            <canvas id="learningTimeChart"></canvas>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 센터별 통계 섹션 -->
            <div id="centers-section" class="section" style="display: none;">
                <div class="page-header-bar">
                    <small class="text-muted fw-bold">Monitoring</small>
                    <h1 class="section-header-title">센터별 통계</h1>
                </div>
                <div class="table-container">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>센터명 (ID)</th>
                                <th>전체 사용자</th>
                                <th>학생 수</th>
                                <th>매니저 수</th>
                                <th>강사 수</th>
                                <th>요금제</th>
                                <th class="text-end">상세보기</th>
                            </tr>
                        </thead>
                        <tbody id="centersTableBody"></tbody>
                    </table>
                </div>
            </div>

            <!-- 권한 설정 섹션 -->
            <div id="permissions-section" class="section" style="display: none;">
                <div class="page-header-bar d-flex justify-content-between align-items-end">
                    <div>
                        <small class="text-muted fw-bold">System</small>
                        <h1 class="section-header-title mb-0">권한 설정</h1>
                    </div>
                    <button class="btn btn-primary rounded-pill px-4" id="savePermissions">
                        <i class="bi bi-check2 me-1"></i> 변경사항 저장
                    </button>
                </div>

                <div class="table-container">
                    <div class="table-responsive">
                        <table class="table table-hover mb-0 align-middle">
                            <thead class="bg-light">
                                <tr>
                                    <th>페이지/기능</th>
                                    <th class="text-center">Admin</th>
                                    <th class="text-center">Kinder</th>
                                    <th class="text-center">School</th>
                                    <th class="text-center">Manager</th>
                                    <th class="text-center">Teacher</th>
                                    <th class="text-center">Student</th>
                                </tr>
                            </thead>
                            <tbody id="permissionsTableBody">
                                <!-- 동적으로 채워질 내용 -->
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            <!-- SQL 관리 섹션 -->
            <div id="sql-manager-section" class="section" style="display: none;">
                <div class="page-header-bar d-flex justify-content-between align-items-end">
                    <div>
                        <small class="text-muted fw-bold">System</small>
                        <h1 class="section-header-title mb-0">SQL 테이블 관리</h1>
                    </div>
                    <div class="d-flex gap-2">
                        <button class="btn btn-sm btn-outline-primary rounded-pill px-3" id="refreshTableBtn">
                            <i class="bi bi-arrow-clockwise me-1"></i> 새로고침
                        </button>
                        <button class="btn btn-sm btn-success rounded-pill px-3" data-bs-toggle="modal"
                            data-bs-target="#csvUploadModal">
                            <i class="bi bi-upload me-1"></i> CSV 업로드
                        </button>
                    </div>
                </div>

                <div class="row g-4">
                    <!-- 좌측: 테이블 목록 -->
                    <div class="col-md-3">
                        <div class="table-container h-100 p-0">
                            <div class="p-3 border-bottom bg-light">
                                <h6 class="mb-0 fw-bold text-muted">테이블 목록</h6>
                            </div>
                            <div class="list-group list-group-flush" id="tableList"
                                style="height: calc(100vh - 400px); overflow-y: auto;">
                            </div>
                        </div>
                    </div>

                    <!-- 우측: 데이터 표시 -->
                    <div class="col-md-9">
                        <div class="table-container h-100 p-0">
                            <div class="p-3 border-bottom bg-light">
                                <h6 class="mb-0 fw-bold text-primary" id="currentTableName">테이블을 선택하세요</h6>
                            </div>
                            <div class="card-body p-0">
                                <!-- 테이블 구조 -->
                                <div id="tableStructureDiv" class="p-3 bg-light border-bottom" style="display: none;">
                                    <h6 class="fw-bold text-muted mb-2" style="font-size: 0.8rem;">테이블 구조</h6>
                                    <div class="table-responsive">
                                        <table class="table table-sm table-bordered bg-white mb-0" id="structureTable"
                                            style="font-size: 0.85rem;">
                                            <thead class="bg-light">
                                                <tr>
                                                    <th>필드</th>
                                                    <th>타입</th>
                                                    <th>Null</th>
                                                    <th>Key</th>
                                                    <th>Default</th>
                                                </tr>
                                            </thead>
                                            <tbody></tbody>
                                        </table>
                                    </div>
                                </div>

                                <!-- 데이터 테이블 -->
                                <div class="table-responsive" style="height: 500px; overflow-y: auto;">
                                    <table class="table table-hover table-sm mb-0 text-nowrap" id="sqlDataTable"
                                        style="font-size: 0.9rem;">
                                        <thead style="position: sticky; top: 0; background-color: #f8f9fa; z-index: 1;">
                                        </thead>
                                        <tbody></tbody>
                                    </table>
                                </div>

                                <!-- 페이지네이션 -->
                                <div class="p-3 border-top bg-light">
                                    <nav id="sqlPagination" style="display: none;">
                                        <ul class="pagination pagination-sm justify-content-center mb-0"></ul>
                                    </nav>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <!-- 활동 상세 모달 -->
    <div class="modal fade" id="activityModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content border-0 shadow-lg" style="border-radius: 16px;">
                <div class="modal-header border-bottom-0">
                    <h5 class="modal-title fw-bold">사용자 활동 상세</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="bg-light">
                                <tr>
                                    <th class="ps-4">시간</th>
                                    <th>활동</th>
                                    <th>URL</th>
                                    <th class="pe-4">IP</th>
                                </tr>
                            </thead>
                            <tbody id="activityModalBody">
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- CSV 업로드 모달 -->
    <div class="modal fade" id="csvUploadModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content border-0 shadow-lg" style="border-radius: 16px;">
                <div class="modal-header border-bottom-0">
                    <h5 class="modal-title fw-bold">CSV 업로드</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label fw-bold small text-muted">업로드할 테이블</label>
                        <input type="text" class="form-control" id="uploadTableName" readonly>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold small text-muted">CSV 파일</label>
                        <input type="file" class="form-control" id="csvFileInput" accept=".csv">
                    </div>
                    <div class="alert alert-warning border-0 bg-warning-subtle text-warning-emphasis">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>
                        기존 데이터는 유지되고 새 데이터가 추가됩니다.
                    </div>
                </div>
                <div class="modal-footer border-top-0">
                    <button type="button" class="btn btn-light rounded-pill" data-bs-dismiss="modal">취소</button>
                    <button type="button" class="btn btn-primary rounded-pill px-4" id="uploadCsvBtn">업로드</button>
                </div>
            </div>
        </div>
    </div>

    <!-- 센터 변경 모달 -->
    <div class="modal fade" id="centerEditModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content border-0 shadow-lg" style="border-radius: 16px;">
                <div class="modal-header border-bottom-0">
                    <h5 class="modal-title fw-bold">소속 센터 변경</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" id="editCenterUserId">
                    <div class="mb-3">
                        <label class="form-label fw-bold small text-muted">사용자</label>
                        <input type="text" class="form-control" id="editCenterUserName" readonly>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold small text-muted">변경할 센터</label>
                        <select class="form-select" id="editCenterSelect">
                            <option value="">센터 선택...</option>
                            <!-- 동적 로드 -->
                        </select>
                    </div>
                </div>
                <div class="modal-footer border-top-0">
                    <button type="button" class="btn btn-light rounded-pill" data-bs-dismiss="modal">취소</button>
                    <button type="button" class="btn btn-primary rounded-pill px-4" id="saveCenterBtn">
                        <i class="bi bi-check2 me-1"></i> 저장
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- 스크립트 -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/admin/dashboard.js"></script>
</body>

</html>