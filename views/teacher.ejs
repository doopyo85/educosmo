<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>코딩앤플레이 - 교사 전용</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/board-layout.css">
    <link rel="stylesheet" href="/css/teacher.css">
</head>

<body>
    <%- include('partials/header') %>

        <div class="board-layout-container">
            <!-- 왼쪽 네비게이션 (Shared Sidebar) -->
            <div class="board-sidebar">
                <div class="board-sidebar-header">
                    <i class="bi bi-person-video3"></i> 교사 전용 메뉴
                </div>

                <!-- 학생 관리 (Submenu) -->
                <div class="board-nav-group mb-2">
                    <div class="board-category-item text-dark fw-bold" style="cursor: default; background: none;">
                        <i class="bi bi-people me-2"></i> 학생 관리
                    </div>
                    <div class="d-flex flex-column gap-1">
                        <a href="/teacher/student-management/progress" class="board-category-item ps-5 py-2"
                            style="border-radius: 0 20px 20px 0; font-size: 0.9rem;">
                            학습 진도
                        </a>
                        <a href="/teacher/student-management/list" class="board-category-item ps-5 py-2"
                            style="border-radius: 0 20px 20px 0; font-size: 0.9rem;">
                            학생 목록
                        </a>
                        <a href="/teacher/student-management/attendance" class="board-category-item ps-5 py-2"
                            style="border-radius: 0 20px 20px 0; font-size: 0.9rem;">
                            출석부
                        </a>
                    </div>
                </div>

                <!-- 센터 블로그 (Center Blog) -->
                <!-- 센터 블로그 (Center Blog) -->
                <div class="board-nav-group mb-2">
                    <div class="board-category-item text-dark fw-bold" style="cursor: default; background: none;">
                        <i class="bi bi-broadcast me-2"></i> 커뮤니티
                    </div>
                    <div class="d-flex flex-column gap-1">
                        <% if (locals.centerID) { %>
                            <a href="/center-blog/<%= centerID %>" target="_blank" class="board-category-item ps-5 py-2"
                                style="border-radius: 0 20px 20px 0; font-size: 0.9rem;">
                                센터 블로그
                                <i class="bi bi-box-arrow-up-right ms-1" style="font-size: 0.7em;"></i>
                            </a>
                            <% } %>
                    </div>
                </div>

                <!-- 수업 자료 (Submenu) -->
                <div class="board-nav-group mb-2">
                    <div class="board-category-item text-dark fw-bold" style="cursor: default; background: none;">
                        <i class="bi bi-journal-text me-2"></i> 수업 자료
                    </div>
                    <div class="d-flex flex-column gap-1">
                        <a href="/teacher/class-materials/lessons" class="board-category-item ps-5 py-2"
                            style="border-radius: 0 20px 20px 0; font-size: 0.9rem;">
                            교안 및 영상
                        </a>
                        <a href="/teacher/class-materials/downloads" class="board-category-item ps-5 py-2"
                            style="border-radius: 0 20px 20px 0; font-size: 0.9rem;">
                            다운로드 폴더
                        </a>
                    </div>
                </div>

                <!-- 진로 진학 (Submenu) -->
                <div class="board-nav-group mb-2">
                    <div class="board-category-item text-dark fw-bold" style="cursor: default; background: none;">
                        <i class="bi bi-signpost-split me-2"></i> 진로 진학
                    </div>
                    <div class="d-flex flex-column gap-1">
                        <a href="/teacher/career-info/university" class="board-category-item ps-5 py-2"
                            style="border-radius: 0 20px 20px 0; font-size: 0.9rem;">
                            대학정보
                        </a>
                        <a href="/teacher/career-info/cutlines" class="board-category-item ps-5 py-2"
                            style="border-radius: 0 20px 20px 0; font-size: 0.9rem;">
                            입결라인
                        </a>
                        <a href="/teacher/career-info/blog" class="board-category-item ps-5 py-2"
                            style="border-radius: 0 20px 20px 0; font-size: 0.9rem;">
                            블로그
                        </a>
                    </div>
                </div>

                <!-- 🔥 구독 상태 카드 -->
                <% if (subscription) { %>
                    <div class="subscription-status-card mt-3 p-3"
                        style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: 15px; color: white;">
                        <div class="d-flex align-items-center justify-content-between mb-2">
                            <div class="fw-bold"><i class="bi bi-star-fill me-2"></i>구독 상태</div>
                            <span class="badge" style="background: rgba(255,255,255,0.3);">
                                <% if (subscription.status==='trial' ) { %>무료 체험<% } %>
                                        <% if (subscription.status==='active' ) { %>활성<% } %>
                                                <% if (subscription.status==='suspended' ) { %>일시정지<% } %>
                                                        <% if (subscription.status==='cancelled' ) { %>취소됨<% } %>
                            </span>
                        </div>
                        <div class="small mb-2">
                            <% if (subscription.plan_type==='trial' ) { %>Trial 플랜<% } %>
                                    <% if (subscription.plan_type==='standard' ) { %>Standard 플랜<% } %>
                                            <% if (subscription.plan_type==='professional' ) { %>Professional 플랜<% } %>
                        </div>
                        <div class="small opacity-75 mb-2">
                            <% if (subscription.status==='trial' ) { %>
                                체험 종료: <%= subscription.trial_ends_at ? new
                                    Date(subscription.trial_ends_at).toLocaleDateString('ko-KR') : '-' %>
                                    <% } else { %>
                                        다음 결제: <%= subscription.next_billing_date ? new
                                            Date(subscription.next_billing_date).toLocaleDateString('ko-KR') : '-' %>
                                            <% } %>
                        </div>
                        <a href="/subscription/manage" class="btn btn-light btn-sm w-100 mt-2"
                            style="font-weight: 600;">
                            <i class="bi bi-gear me-1"></i>구독 관리
                        </a>
                    </div>
                    <% } %>
            </div>

            <!-- 메인 콘텐츠 영역 -->
            <div class="board-main-content">
                <div id="content-container">
                    <div class="loading-message">
                        <div class="spinner-border text-primary" role="status">
                            <span class="visually-hidden">Loading...</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Scripts -->
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <script src="/js/teacher/teacher-main.js"></script>
        <!-- 🔥 Bootstrap 무시하고 직접 드롭다운 제어 -->
        <script>
            (function() {
                // 즉시 실행 - DOM 로드 기다리지 않음
                function setupCustomDropdown() {
                    const profileDropdown = document.getElementById('profileDropdown');
                    // 🔥 정확한 셀렉터: profileDropdown의 바로 다음 형제 요소인 dropdown-menu
                    const dropdownMenu = profileDropdown ? profileDropdown.nextElementSibling : null;

                    if (!profileDropdown || !dropdownMenu || !dropdownMenu.classList.contains('dropdown-menu')) {
                        // 요소가 없으면 50ms 후 재시도
                        setTimeout(setupCustomDropdown, 50);
                        return;
                    }

                    // 이미 설정되었으면 중복 방지
                    if (profileDropdown.dataset.customDropdownSetup) {
                        return;
                    }
                    profileDropdown.dataset.customDropdownSetup = 'true';

                    // Bootstrap 드롭다운 완전히 비활성화
                    profileDropdown.removeAttribute('data-bs-toggle');
                    profileDropdown.setAttribute('data-bs-toggle-disabled', 'dropdown');

                    // 직접 클릭 이벤트 처리
                    profileDropdown.addEventListener('click', function(e) {
                        e.preventDefault();
                        e.stopPropagation();
                        e.stopImmediatePropagation(); // 다른 모든 이벤트 차단

                        const isOpen = dropdownMenu.classList.contains('show');

                        if (isOpen) {
                            // 닫기
                            dropdownMenu.classList.remove('show');
                            profileDropdown.setAttribute('aria-expanded', 'false');
                        } else {
                            // 열기
                            dropdownMenu.classList.add('show');
                            profileDropdown.setAttribute('aria-expanded', 'true');
                        }
                    });

                    // 외부 클릭 시 닫기
                    document.addEventListener('click', function(e) {
                        if (!profileDropdown.contains(e.target) && !dropdownMenu.contains(e.target)) {
                            dropdownMenu.classList.remove('show');
                            profileDropdown.setAttribute('aria-expanded', 'false');
                        }
                    });

                    console.log('Custom dropdown setup complete for profile');
                }

                // 즉시 실행
                setupCustomDropdown();
            })();
        </script>
</body>

</html>