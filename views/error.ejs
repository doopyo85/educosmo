<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>오류 발생</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.5.0/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <%- include('partials/header') %>

        <div class="container mt-5">
            <div class="row justify-content-center">
                <div class="col-md-8">
                    <div class="card border-danger shadow-sm">
                        <div class="card-header bg-danger text-white">
                            <h4 class="mb-0"><i class="bi bi-exclamation-triangle-fill me-2"></i>오류 발생</h4>
                        </div>
                        <div class="card-body">
                            <div class="alert alert-light border-0">
                                <h5 class="alert-heading fw-bold mb-3">
                                    <%= message || '알 수 없는 오류가 발생했습니다.' %>
                                </h5>
                                <% if (typeof error !=='undefined' && error) { %>
                                    <div class="bg-light p-3 rounded font-monospace text-danger small"
                                        style="white-space: pre-wrap;">
                                        <%= typeof error==='object' ? (error.stack || JSON.stringify(error, null, 2)) :
                                            error %>
                                    </div>
                                    <% } %>
                            </div>

                            <div class="mt-4 d-flex justify-content-end gap-2">
                                <button onclick="history.back()" class="btn btn-secondary">
                                    <i class="bi bi-arrow-left"></i> 뒤로 가기
                                </button>
                                <a href="/" class="btn btn-primary">
                                    <i class="bi bi-house-door"></i> 홈으로
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <script src="/header.js"></script>
</body>

</html>