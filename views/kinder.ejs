<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>교사게시판</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.5.0/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <script src="https://apis.google.com/js/api.js"></script>   
</head>
<body>
    <%- include('partials/header') %>

    <div class="container mt-5">
        <h1 class="text-center">교사게시판</h1>
        <p class="text-center">교사들이 필요한 자료를 모아놓았습니다.</p>

        <!-- 동적으로 탭 생성 -->
        <ul class="nav nav-tabs" id="categoryTabs" role="tablist">
            <li class="nav-item">
                <button class="nav-link active" id="tab-preschool" data-bs-toggle="tab" data-bs-target="#preschool" type="button" role="tab" aria-selected="true">
                    <%= preschoolTitle %>
                </button>
            </li>
            <li class="nav-item">
                <button class="nav-link" id="tab-preschoolAI" data-bs-toggle="tab" data-bs-target="#preschoolAI" type="button" role="tab" aria-selected="false">
                    <%= preschoolAITitle %>
                </button>
            </li>
        </ul>

        <!-- 탭 콘텐츠 -->
        <div class="tab-content mt-3">
            <div id="preschool" class="tab-pane fade show active">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>종류</th>
                            <th>내용</th>
                            <th>다운로드 링크</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% preschoolItems.forEach(item => { %>
                            <tr>
                                <td><%= item.type %></td>
                                <td><%= item.content %></td>
                                <td>
                                    <% item.links.forEach(linkText => { %>
                                        <a href="<%= item.url %>" target="_blank"><%= linkText %></a><br>
                                    <% }); %>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>

            <div id="preschoolAI" class="tab-pane fade">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>종류</th>
                            <th>내용</th>
                            <th>다운로드 링크</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% preschoolAIItems.forEach(item => { %>
                            <tr>
                                <td><%= item.type %></td>
                                <td><%= item.content %></td>
                                <td>
                                    <% item.links.forEach(linkText => { %>
                                        <a href="<%= item.url %>" target="_blank"><%= linkText %></a><br>
                                    <% }); %>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
