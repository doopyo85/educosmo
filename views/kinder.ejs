<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>교사게시판</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.5.0/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <script src="https://apis.google.com/js/api.js"></script>
</head>

<body>
    <%- include('partials/header') %>

        <div class="container mt-5">
            <h1 class="text-center">교사게시판</h1>
            <p class="text-center">교사들이 필요한 자료를 모아놓았습니다.</p>

            <!-- 동적으로 탭 생성 -->
            <ul class="nav nav-pills justify-content-center mb-4" id="categoryTabs" role="tablist">
                <li class="nav-item">
                    <button class="nav-link active" id="tab-preschool" data-bs-toggle="tab" data-bs-target="#preschool"
                        type="button" role="tab" aria-selected="true">
                        <%= preschoolTitle %>
                    </button>
                </li>
                <li class="nav-item">
                    <button class="nav-link" id="tab-preschoolAI" data-bs-toggle="tab" data-bs-target="#preschoolAI"
                        type="button" role="tab" aria-selected="false">
                        <%= preschoolAITitle %>
                    </button>
                </li>
            </ul>

            <!-- 탭 콘텐츠 -->
            <div class="tab-content mt-3">
                <style>
                    /* Custom Styles for Kinder Page */
                    .kinder-card {
                        transition: transform 0.2s, box-shadow 0.2s;
                        border: none;
                        border-radius: 15px;
                        overflow: hidden;
                        background: #fff;
                    }

                    .kinder-card:hover {
                        transform: translateY(-5px);
                        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1) !important;
                    }

                    .kinder-card .card-header {
                        background: rgba(var(--bs-primary-rgb), 0.05);
                        /* Very light primary */
                        border-bottom: none;
                        font-weight: 600;
                        color: var(--bs-primary);
                        padding: 1rem 1.25rem;
                    }

                    .kinder-card .card-body {
                        padding: 1.5rem 1.25rem;
                    }

                    .kinder-card .card-footer {
                        background: transparent;
                        border-top: 1px solid rgba(0, 0, 0, 0.05);
                        padding: 1rem 1.25rem;
                    }

                    .btn-download {
                        border-radius: 20px;
                        font-size: 0.9rem;
                        transition: all 0.2s;
                    }

                    .btn-download:hover {
                        background-color: var(--bs-primary);
                        color: white;
                    }

                    .nav-pills .nav-link {
                        border-radius: 20px;
                        padding: 10px 25px;
                        font-weight: 500;
                        margin-right: 10px;
                    }
                </style>

                <!-- Cards Grid -->
                <div class="row row-cols-1 row-cols-md-2 row-cols-xl-4 g-4">
                    <% preschoolItems.forEach(item=> { %>
                        <div class="col">
                            <div class="card h-100 shadow-sm kinder-card">
                                <div class="card-header d-flex justify-content-between align-items-center">
                                    <span class="badge bg-primary-subtle text-primary-emphasis rounded-pill">
                                        <%= item.type %>
                                    </span>
                                    <i class="bi bi-folder2-open text-muted" style="font-size: 1.2rem;"></i>
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title mb-0 fw-bold" style="color: #444;">
                                        <%= item.content %>
                                    </h5>
                                </div>
                                <div class="card-footer">
                                    <div class="d-grid gap-2">
                                        <% item.links.forEach((linkText, idx)=> { %>
                                            <% if (linkText.trim()) { %>
                                                <a href="<%= item.url %>" target="_blank"
                                                    class="btn btn-outline-primary btn-sm btn-download text-start">
                                                    <i class="bi bi-file-earmark-arrow-down me-2"></i>
                                                    <%= linkText %>
                                                </a>
                                                <% } %>
                                                    <% }); %>
                                                        <% if (item.links.length===0) { %>
                                                            <button class="btn btn-light btn-sm text-muted" disabled>자료
                                                                없음</button>
                                                            <% } %>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <% }); %>
                </div>
            </div>

            <div id="preschoolAI" class="tab-pane fade">
                <!-- Cards Grid for AI -->
                <div class="row row-cols-1 row-cols-md-2 row-cols-xl-4 g-4">
                    <% preschoolAIItems.forEach(item=> { %>
                        <div class="col">
                            <div class="card h-100 shadow-sm kinder-card">
                                <div class="card-header d-flex justify-content-between align-items-center">
                                    <span class="badge bg-success-subtle text-success-emphasis rounded-pill">
                                        <%= item.type %>
                                    </span>
                                    <i class="bi bi-robot text-muted" style="font-size: 1.2rem;"></i>
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title mb-0 fw-bold" style="color: #444;">
                                        <%= item.content %>
                                    </h5>
                                </div>
                                <div class="card-footer">
                                    <div class="d-grid gap-2">
                                        <% item.links.forEach((linkText, idx)=> { %>
                                            <% if (linkText.trim()) { %>
                                                <a href="<%= item.url %>" target="_blank"
                                                    class="btn btn-outline-success btn-sm btn-download text-start">
                                                    <i class="bi bi-cloud-arrow-down me-2"></i>
                                                    <%= linkText %>
                                                </a>
                                                <% } %>
                                                    <% }); %>
                                                        <% if (item.links.length===0) { %>
                                                            <button class="btn btn-light btn-sm text-muted" disabled>자료
                                                                없음</button>
                                                            <% } %>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <% }); %>
                </div>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>