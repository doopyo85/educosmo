<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>동물 잡기 (마우스 클릭 연습) - <%= locals.serviceName || '코딩앤플레이' %>
    </title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/pong2/click-practice.css">
</head>

<body>
    <!-- 헤더 -->
    <%- include('partials/header') %>

        <div class="container game-container">
            <h1 class="text-center mb-4">
                <i class="bi bi-mouse"></i> 동물 잡기
            </h1>
            <p class="text-center text-muted mb-4">
                빠르게 나타나는 동물들을 클릭해서 점수를 획득하세요!<br>
                🐯(+50), 🦊(+30), 🐰(+20), 🐭(+10), 💣(-50)
            </p>

            <!-- Game Header (Score & Time) -->
            <div class="game-header">
                <div class="score-display">
                    점수: <span id="score">0</span>
                </div>
                <div class="time-display">
                    시간: <span id="time">30</span>초
                </div>
            </div>

            <!-- Canvas -->
            <div class="canvas-wrapper">
                <canvas id="gameCanvas" width="600" height="600"></canvas>

                <!-- Game Over Overlay -->
                <div id="overlay" class="game-overlay d-none">
                    <div id="overlay-title" class="overlay-title">Time's Up!</div>
                    <div id="overlay-score" class="overlay-score"></div>
                </div>
            </div>

            <!-- Controls -->
            <div class="game-controls">
                <button id="start-btn" class="btn btn-primary start-btn">
                    <i class="bi bi-play-fill"></i> 게임 시작
                </button>
                <a href="/machu" class="btn btn-secondary ms-2" style="padding: 12px 20px; border-radius: 50px;">
                    <i class="bi bi-arrow-left"></i> 목록으로
                </a>
            </div>

            <!-- Ranking -->
            <div class="ranking-section">
                <h3 class="ranking-title">🏆 명예의 전당</h3>
                <table class="ranking-table">
                    <thead>
                        <tr>
                            <th width="20%">순위</th>
                            <th width="50%">이름</th>
                            <th width="30%">점수</th>
                        </tr>
                    </thead>
                    <tbody id="ranking-body">
                        <tr>
                            <td colspan="3">랭킹을 불러오는 중...</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- 푸터 -->
        <%- include('partials/footer') %>

            <!-- JavaScript -->
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
            <script src="/js/pong2/click-practice.js"></script>
</body>

</html>