<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= post.title %> - <%= post.category_name %>
    </title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/board-layout.css">
    <style>
        /* 🔥 게시글 상세보기 전용 스타일 */
        body {
            background-color: #f8f9fa;
            font-family: 'Noto Sans KR', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
        }

        /* 카테고리 배지 */
        .post-category-badge {
            display: inline-block;
            background-color: #4a90e2;
            color: white;
            padding: 6px 12px;
            border-radius: 16px;
            font-size: 12px;
            font-weight: 500;
            margin-bottom: 15px;
        }

        /* 제목 */
        .post-title {
            font-size: 20px;
            font-weight: bold;
            color: #333;
            margin-bottom: 0;
            line-height: 1.4;
        }

        .post-number {
            color: #e74c3c;
            font-size: 18px;
            margin-left: 8px;
        }

        /* 메타 정보 */
        .post-meta {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 12px 0;
            border-bottom: 1px solid #f0f0f0;
            font-size: 13px;
            color: #666;
        }

        .meta-item {
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .meta-item i {
            font-size: 12px;
        }

        /* 게시글 내용 */
        .post-content {
            padding: 30px 30px 20px 30px;
            line-height: 1.8;
            font-size: 15px;
            color: #333;
            min-height: 300px;
        }

        .post-content p {
            margin-bottom: 16px;
        }

        .post-content img {
            max-width: 100%;
            height: auto;
            margin: 15px 0;
            border-radius: 4px;
        }

        /* 게시글 헤더 */
        .post-header {
            padding: 25px 30px 15px 30px;
            border-bottom: 1px solid #f0f0f0;
        }

        /* 🔥 헤더 상단 영역 (카테고리 + 액션 버튼) */
        .post-header-top {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        /* 🔥 타이틀 영역 (제목 + 수정/삭제 버튼) */
        .post-title-section {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 20px;
        }

        /* 🔥 액션 버튼 그룹 */
        .post-action-buttons {
            display: flex;
            gap: 8px;
            flex-shrink: 0;
        }

        .post-action-btn {
            padding: 6px 12px;
            font-size: 12px;
            font-weight: 500;
            border-radius: 4px;
            text-decoration: none;
            border: 1px solid;
            transition: all 0.2s ease;
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .post-action-btn-edit {
            background-color: #fff;
            color: #6c757d;
            border-color: #6c757d;
        }

        .post-action-btn-edit:hover {
            background-color: #6c757d;
            color: white;
            text-decoration: none;
        }

        .post-action-btn-delete {
            background-color: #fff;
            color: #6c757d;
            border-color: #6c757d;
            cursor: pointer;
        }

        .post-action-btn-delete:hover {
            background-color: #6c757d;
            color: white;
        }

        /* 🔥 하단 액션 영역 제거됨 - 헤더로 이동 */

        /* 🔥 개선된 첨부파일 섹션 */
        .attachments-section {
            padding: 25px 30px;
            background-color: #f8f9fa;
            border-top: 1px solid #e9ecef;
        }

        .attachments-header {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 15px;
            font-size: 16px;
            font-weight: 600;
            color: #495057;
        }

        .attachments-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 12px;
        }

        .attachment-item {
            display: flex;
            align-items: center;
            padding: 12px;
            background-color: #fff;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            transition: all 0.2s ease;
            position: relative;
        }

        .attachment-item:hover {
            border-color: #4a90e2;
            box-shadow: 0 2px 8px rgba(74, 144, 226, 0.15);
            transform: translateY(-1px);
        }

        .attachment-icon {
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 8px;
            margin-right: 12px;
            font-size: 18px;
            color: white;
            flex-shrink: 0;
        }

        .attachment-icon.image {
            background: linear-gradient(135deg, #28a745, #20c997);
        }

        .attachment-icon.document {
            background: linear-gradient(135deg, #007bff, #6610f2);
        }

        .attachment-icon.archive {
            background: linear-gradient(135deg, #ffc107, #fd7e14);
            color: #212529;
        }

        .attachment-icon.pdf {
            background: linear-gradient(135deg, #dc3545, #e83e8c);
        }

        .attachment-icon.excel {
            background: linear-gradient(135deg, #198754, #20c997);
        }

        .attachment-icon.word {
            background: linear-gradient(135deg, #0d6efd, #6f42c1);
        }

        .attachment-icon.powerpoint {
            background: linear-gradient(135deg, #fd7e14, #ffc107);
        }

        .attachment-icon.other {
            background: linear-gradient(135deg, #6c757d, #adb5bd);
        }

        .attachment-info {
            flex: 1;
            min-width: 0;
        }

        .attachment-name {
            font-weight: 500;
            color: #333;
            margin-bottom: 4px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            font-size: 14px;
        }

        .attachment-meta {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 12px;
            color: #6c757d;
        }

        .attachment-size {
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .attachment-downloads {
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .attachment-actions {
            display: flex;
            gap: 6px;
        }

        .attachment-btn {
            width: 32px;
            height: 32px;
            border: none;
            border-radius: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.2s ease;
            font-size: 14px;
            text-decoration: none;
        }

        .attachment-btn.download {
            background-color: #e3f2fd;
            color: #1976d2;
        }

        .attachment-btn.download:hover {
            background-color: #bbdefb;
            color: #0d47a1;
            text-decoration: none;
        }

        .attachment-btn.preview {
            background-color: #f3e5f5;
            color: #7b1fa2;
        }

        .attachment-btn.preview:hover {
            background-color: #e1bee7;
            color: #4a148c;
        }

        .attachment-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        /* 🔥 이미지 미리보기 모달 */
        .image-preview-modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            z-index: 9999;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
        }

        .image-preview-modal.show {
            opacity: 1;
            visibility: visible;
        }

        .image-preview-container {
            position: relative;
            max-width: 90vw;
            max-height: 90vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .image-preview-img {
            max-width: 100%;
            max-height: 100%;
            object-fit: contain;
            border-radius: 8px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
        }

        .image-preview-close {
            position: absolute;
            top: -40px;
            right: -40px;
            width: 40px;
            height: 40px;
            background: rgba(255, 255, 255, 0.9);
            border: none;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            color: #333;
            transition: all 0.2s ease;
        }

        .image-preview-close:hover {
            background: white;
            transform: scale(1.1);
        }

        .image-preview-info {
            position: absolute;
            bottom: -50px;
            left: 0;
            right: 0;
            text-align: center;
            color: white;
            font-size: 14px;
        }

        /* 🔥 다운로드 진행률 표시 */
        .download-progress {
            position: fixed;
            top: 20px;
            right: 20px;
            background: rgba(0, 0, 0, 0.8);
            color: white;
            padding: 15px 20px;
            border-radius: 8px;
            z-index: 9998;
            display: none;
            min-width: 250px;
        }

        .download-progress-text {
            margin-bottom: 8px;
            font-size: 14px;
        }

        .download-progress-bar {
            width: 100%;
            height: 6px;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 3px;
            overflow: hidden;
        }

        .download-progress-fill {
            height: 100%;
            background: #28a745;
            width: 0%;
            transition: width 0.3s ease;
        }

        /* 댓글 섹션 */
        .comments-section {
            padding: 30px;
            border-top: 1px solid #f0f0f0;
        }

        .comments-header {
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #f0f0f0;
            font-size: 16px;
            font-weight: 600;
        }

        /* 🔥 댓글 폼 재설계 */
        .comment-form {
            margin-top: 20px;
            padding: 20px;
            background-color: #f8f9fa;
            border-radius: 8px;
        }

        .comment-input-container {
            position: relative;
        }

        .comment-textarea {
            width: 100%;
            border: 1px solid #dee2e6;
            border-radius: 6px;
            padding: 12px;
            font-size: 14px;
            line-height: 1.5;
            resize: vertical;
            min-height: 80px;
        }

        .comment-textarea:focus {
            outline: none;
            border-color: #80bdff;
            box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
        }

        /* 🔥 댓글 등록 버튼 (수정/삭제와 동일 스타일) */
        .comment-submit-container {
            display: flex;
            justify-content: flex-end;
            margin-top: 10px;
        }

        .comment-submit-btn {
            padding: 6px 12px;
            font-size: 12px;
            font-weight: 500;
            border-radius: 4px;
            text-decoration: none;
            border: 1px solid #6c757d;
            background-color: #fff;
            color: #6c757d;
            cursor: pointer;
            transition: all 0.2s ease;
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .comment-submit-btn:hover {
            background-color: #6c757d;
            color: white;
        }

        /* 반응형 조정 */
        @media (max-width: 768px) {
            .post-header {
                padding-left: 20px;
                padding-right: 20px;
            }

            .post-content {
                padding-left: 20px;
                padding-right: 20px;
                padding-top: 20px;
                padding-bottom: 15px;
            }

            .attachments-section,
            .comments-section {
                padding-left: 20px;
                padding-right: 20px;
            }

            .post-meta {
                flex-wrap: wrap;
                gap: 10px;
            }

            /* 🔥 모바일에서 타이틀과 버튼 세로 배치 */
            .post-title-section {
                flex-direction: column;
                align-items: flex-start;
                gap: 10px;
            }

            .post-action-buttons {
                align-self: flex-end;
            }
        }
    </style>
</head>

<body>
    <%- include('../partials/header') %>

        <div class="board-layout-container">
            <!-- 🔥 왼쪽 네비게이션 (항상 고정) -->
            <div class="board-sidebar">
                <div class="board-sidebar-header">
                    <i class="bi bi-grid-3x3-gap"></i> 게시판
                </div>

                <a href="/board" class="board-category-item">
                    <i class="bi bi-house"></i> 전체 게시판
                </a>

                <a href="/board/notice"
                    class="board-category-item <%= post.category_slug === 'notice' ? 'active' : '' %>">
                    <i class="bi bi-megaphone"></i> 업데이트 노트
                </a>

                <a href="/board/education"
                    class="board-category-item <%= post.category_slug === 'education' ? 'active' : '' %>">
                    <i class="bi bi-book"></i> 교육정보
                </a>

                <a href="/board/free" class="board-category-item <%= post.category_slug === 'free' ? 'active' : '' %>">
                    <i class="bi bi-chat-dots"></i> 자유게시판
                </a>
            </div>

            <!-- 🔥 메인 콘텐츠 (고정 너비) -->
            <div class="board-main-content">
                <!-- 게시글 헤더 -->
                <div class="post-header">
                    <!-- 🔥 헤더 상단: 카테고리 뱃지 -->
                    <div class="post-header-top">
                        <div class="post-category-badge">
                            <%= post.category_name %>
                        </div>
                    </div>

                    <!-- 🔥 타이틀 섹션: 제목 + 액션 버튼 -->
                    <div class="post-title-section">
                        <h1 class="post-title">
                            <%= post.title %>
                                <span class="post-number">
                                    <%= post.id %>
                                </span>
                        </h1>

                        <!-- 🔥 수정/삭제 버튼 (권한이 있을 때만 표시) -->
                        <% if (canEdit || canDelete) { %>
                            <div class="post-action-buttons">
                                <% if (canEdit) { %>
                                    <a href="/board/<%= post.category_slug %>/<%= post.id %>/edit"
                                        class="post-action-btn post-action-btn-edit">
                                        <i class="bi bi-pencil"></i> 수정
                                    </a>
                                    <% } %>
                                        <% if (canDelete) { %>
                                            <button type="button" class="post-action-btn post-action-btn-delete"
                                                onclick="deletePost()">
                                                <i class="bi bi-trash"></i> 삭제
                                            </button>
                                            <% } %>
                            </div>
                            <% } %>
                    </div>

                    <!-- 메타 정보 -->
                    <div class="post-meta">
                        <div class="meta-item">
                            <i class="bi bi-eye"></i>
                            <span>
                                <%= post.views %>
                            </span>
                        </div>
                        <div class="meta-item">
                            <i class="bi bi-calendar3"></i>
                            <span>
                                <%= new Date(post.created_at).toLocaleString('ko-KR', { timeZone: 'Asia/Seoul' }) %>
                            </span>
                        </div>
                        <div class="meta-item">
                            <i class="bi bi-heart"></i>
                            <span>221</span>
                        </div>
                        <div class="meta-item">
                            <span>53</span>
                        </div>
                    </div>
                </div>

                <!-- 게시글 내용 -->
                <div class="post-content">
                    <%- post.content.replace(/\n/g, '<br>' ) %>
                </div>

                <!-- 🔥 첨부파일 섹션 -->
                <% if (attachments && attachments.length> 0) { %>
                    <div class="attachments-section">
                        <div class="attachments-header">
                            <i class="bi bi-paperclip"></i>
                            첨부파일
                            <span class="badge bg-primary rounded-pill ms-2">
                                <%= attachments.length %>
                            </span>
                        </div>

                        <div class="attachments-grid">
                            <% attachments.forEach(attachment=> { %>
                                <div class="attachment-item" data-attachment-id="<%= attachment.id %>">
                                    <!-- 파일 타입별 아이콘 -->
                                    <div
                                        class="attachment-icon <%= getFileTypeClass(attachment.file_type, attachment.original_name) %>">
                                        <i
                                            class="bi <%= getFileIcon(attachment.file_type, attachment.original_name) %>"></i>
                                    </div>

                                    <!-- 파일 정보 -->
                                    <div class="attachment-info">
                                        <div class="attachment-name" title="<%= attachment.original_name %>">
                                            <%= attachment.original_name %>
                                        </div>
                                        <div class="attachment-meta">
                                            <div class="attachment-size">
                                                <i class="bi bi-hdd"></i>
                                                <%= formatFileSize(attachment.file_size) %>
                                            </div>
                                            <div class="attachment-downloads">
                                                <i class="bi bi-download"></i>
                                                <span class="download-count"
                                                    data-attachment-id="<%= attachment.id %>">-</span>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- 액션 버튼 -->
                                    <div class="attachment-actions">
                                        <% // 이미지 파일 체크 개선 const isImageFile=attachment.file_type &&
                                            attachment.file_type.startsWith('image/'); %>
                                            <% if (isImageFile) { %>
                                                <button class="attachment-btn preview"
                                                    onclick="previewImage('<%= attachment.s3_url %>', '<%= attachment.original_name %>')"
                                                    title="미리보기">
                                                    <i class="bi bi-eye"></i>
                                                </button>
                                                <% } %>
                                                    <button class="attachment-btn download"
                                                        onclick="downloadFile('<%= attachment.id %>', '<%= attachment.original_name %>')"
                                                        title="다운로드">
                                                        <i class="bi bi-download"></i>
                                                    </button>
                                    </div>
                                </div>
                                <% }); %>
                        </div>
                    </div>
                    <% } %>

                        <!-- 🔥 이미지 미리보기 모달 -->
                        <div class="image-preview-modal" id="imagePreviewModal">
                            <div class="image-preview-container">
                                <img class="image-preview-img" id="imagePreviewImg" src="" alt="">
                                <button class="image-preview-close" onclick="closeImagePreview()">
                                    <i class="bi bi-x"></i>
                                </button>
                                <div class="image-preview-info" id="imagePreviewInfo"></div>
                            </div>
                        </div>

                        <!-- 🔥 다운로드 진행률 표시 -->
                        <div class="download-progress" id="downloadProgress">
                            <div class="download-progress-text" id="downloadProgressText">파일 준비 중...</div>
                            <div class="download-progress-bar">
                                <div class="download-progress-fill" id="downloadProgressFill"></div>
                            </div>
                        </div>

                        <!-- 댓글 섹션 -->
                        <div class="comments-section">
                            <div class="comments-header">
                                <i class="bi bi-chat-dots"></i> 댓글
                            </div>

                            <!-- 댓글 목록 -->
                            <div class="comments-list">
                                <div class="text-center text-muted py-4">
                                    첫 번째 댓글을 작성해보세요!
                                </div>
                            </div>

                            <!-- 댓글 작성 폼 -->
                            <% if (is_logined) { %>
                                <div class="comment-form">
                                    <form id="commentForm">
                                        <div class="comment-input-container">
                                            <textarea class="comment-textarea" id="commentContent" rows="3"
                                                placeholder="댓글을 입력하세요..." maxlength="500"></textarea>
                                        </div>
                                        <div class="comment-submit-container">
                                            <button type="submit" class="comment-submit-btn">
                                                <i class="bi bi-send"></i> 댓글 등록
                                            </button>
                                        </div>
                                    </form>
                                </div>
                                <% } else { %>
                                    <div class="comment-form">
                                        <div class="text-center text-muted py-3">
                                            댓글을 작성하려면 <a href="/auth/login">로그인</a>이 필요합니다.
                                        </div>
                                    </div>
                                    <% } %>
                        </div>

                        <!-- 🔥 하단 액션 버튼 영역 제거됨 (헤더로 이동) -->
            </div>

            <!-- 🔥 오른쪽 액션 버튼 그룹 (항상 고정) -->
            <div class="board-action-buttons">
                <a href="/board/<%= post.category_slug %>/write" class="board-floating-btn" title="글쓰기">
                    <i class="bi bi-pencil-square"></i>
                    <span class="btn-text">글쓰기</span>
                </a>

                <a href="/board/<%= post.category_slug %>" class="board-floating-btn" title="목록">
                    <i class="bi bi-list"></i>
                    <span class="btn-text">목록</span>
                </a>

                <a href="#" class="board-floating-btn" id="prevBtn" title="이전글">
                    <i class="bi bi-chevron-up"></i>
                    <span class="btn-text">이전글</span>
                </a>

                <a href="#" class="board-floating-btn" id="nextBtn" title="다음글">
                    <i class="bi bi-chevron-down"></i>
                    <span class="btn-text">다음글</span>
                </a>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <script>
            // 게시글 삭제
            function deletePost() {
                if (confirm('정말 삭제하시겠습니까?\n삭제된 게시글은 복구할 수 없습니다.')) {
                    fetch(`/api/board/posts/<%= post.id %>`, {
                        method: 'DELETE',
                        headers: {
                            'Content-Type': 'application/json',
                        }
                    })
                        .then(response => response.json())
                        .then(data => {
                            if (data.success) {
                                alert('게시글이 삭제되었습니다.');
                                location.href = '/board/<%= post.category_slug %>';
                            } else {
                                alert('삭제 중 오류가 발생했습니다: ' + (data.error || '알 수 없는 오류'));
                            }
                        })
                        .catch(error => {
                            console.error('삭제 오류:', error);
                            alert('삭제 중 오류가 발생했습니다.');
                        });
                }
            }

            // 이전글/다음글 네비게이션
            async function loadNavigation() {
                try {
                    const response = await fetch(`/api/board/posts/<%= post.id %>/navigation`);
                    if (response.ok) {
                        const data = await response.json();

                        const prevBtn = document.getElementById('prevBtn');
                        const nextBtn = document.getElementById('nextBtn');

                        if (data.prev) {
                            prevBtn.href = `/board/<%= post.category_slug %>/${data.prev.id}`;
                            prevBtn.title = `이전글: ${data.prev.title}`;
                        } else {
                            prevBtn.classList.add('disabled');
                            prevBtn.title = '이전글이 없습니다';
                        }

                        if (data.next) {
                            nextBtn.href = `/board/<%= post.category_slug %>/${data.next.id}`;
                            nextBtn.title = `다음글: ${data.next.title}`;
                        } else {
                            nextBtn.classList.add('disabled');
                            nextBtn.title = '다음글이 없습니다';
                        }
                    }
                } catch (error) {
                    console.error('네비게이션 로딩 오류:', error);
                }
            }

            // 댓글 작성
            document.getElementById('commentForm')?.addEventListener('submit', function (e) {
                e.preventDefault();

                const content = document.getElementById('commentContent').value.trim();
                if (!content) {
                    alert('댓글 내용을 입력해주세요.');
                    return;
                }

                // TODO: 댓글 작성 API 구현
                alert('댓글 기능은 준비 중입니다.');
            });

            // 키보드 단축키
            document.addEventListener('keydown', function (e) {
                // ESC: 목록으로 돌아가기
                if (e.key === 'Escape') {
                    location.href = '/board/<%= post.category_slug %>';
                }

                // 화살표 키로 이전글/다음글 이동
                if (e.key === 'ArrowUp' && e.ctrlKey) {
                    e.preventDefault();
                    document.getElementById('prevBtn').click();
                }
                if (e.key === 'ArrowDown' && e.ctrlKey) {
                    e.preventDefault();
                    document.getElementById('nextBtn').click();
                }
            });

            // 이미지 클릭 시 원본 크기로 보기
            document.querySelectorAll('.post-content img').forEach(img => {
                img.addEventListener('click', function () {
                    window.open(this.src, '_blank');
                });
                img.style.cursor = 'pointer';
                img.title = '클릭하면 원본 크기로 볼 수 있습니다';
            });

            // 🔥 첨부파일 관련 함수들

            // 파일 다운로드
            async function downloadFile(attachmentId, filename) {
                try {
                    console.log('다운로드 시작:', { attachmentId, filename });
                    showDownloadProgress('파일 준비 중...');

                    // 다운로드 URL 요청
                    const response = await fetch(`/api/board/attachments/${attachmentId}/download`);

                    console.log('다운로드 API 응답 상태:', response.status);

                    if (!response.ok) {
                        const errorResult = await response.json();
                        throw new Error(errorResult.error || `서버 오류 (${response.status})`);
                    }

                    const result = await response.json();
                    console.log('다운로드 결과:', result);

                    if (result.success && result.download_url) {
                        updateDownloadProgress('다운로드 시작...', 50);

                        // 실제 파일 다운로드
                        const link = document.createElement('a');
                        link.href = result.download_url;
                        link.download = filename;
                        link.style.display = 'none';
                        document.body.appendChild(link);
                        link.click();
                        document.body.removeChild(link);

                        updateDownloadProgress('다운로드 완료!', 100);

                        // 다운로드 카운터 업데이트
                        updateDownloadCount(attachmentId);

                        setTimeout(hideDownloadProgress, 2000);
                    } else {
                        throw new Error(result.error || '다운로드 URL을 가져올 수 없습니다.');
                    }

                } catch (error) {
                    console.error('파일 다운로드 오류:', error);
                    hideDownloadProgress();
                    alert('파일 다운로드 중 오류가 발생했습니다: ' + error.message);
                }
            }

            // 이미지 미리보기
            function previewImage(imageUrl, filename) {
                console.log('이미지 미리보기:', { imageUrl, filename });

                const modal = document.getElementById('imagePreviewModal');
                const img = document.getElementById('imagePreviewImg');
                const info = document.getElementById('imagePreviewInfo');

                if (!modal || !img || !info) {
                    console.error('미리보기 요소를 찾을 수 없습니다');
                    return;
                }

                img.src = imageUrl;
                img.alt = filename;
                info.textContent = filename;

                modal.classList.add('show');
                document.body.style.overflow = 'hidden'; // 스크롤 방지

                // ESC 키로 닫기
                document.addEventListener('keydown', handlePreviewKeydown);
            }

            // 이미지 미리보기 닫기
            function closeImagePreview() {
                const modal = document.getElementById('imagePreviewModal');
                if (modal) {
                    modal.classList.remove('show');
                    document.body.style.overflow = ''; // 스크롤 복원
                }
                document.removeEventListener('keydown', handlePreviewKeydown);
            }

            // 미리보기 키보드 이벤트
            function handlePreviewKeydown(e) {
                if (e.key === 'Escape') {
                    closeImagePreview();
                }
            }

            // 다운로드 진행률 표시
            function showDownloadProgress(text) {
                const progress = document.getElementById('downloadProgress');
                const progressText = document.getElementById('downloadProgressText');
                const progressFill = document.getElementById('downloadProgressFill');

                progressText.textContent = text;
                progressFill.style.width = '0%';
                progress.style.display = 'block';
            }

            function updateDownloadProgress(text, percent) {
                const progressText = document.getElementById('downloadProgressText');
                const progressFill = document.getElementById('downloadProgressFill');

                progressText.textContent = text;
                progressFill.style.width = percent + '%';
            }

            function hideDownloadProgress() {
                const progress = document.getElementById('downloadProgress');
                progress.style.display = 'none';
            }

            // 다운로드 카운터 업데이트
            async function updateDownloadCount(attachmentId) {
                try {
                    const countElement = document.querySelector(`[data-attachment-id="${attachmentId}"] .download-count`);
                    if (countElement) {
                        const currentCount = parseInt(countElement.textContent) || 0;
                        countElement.textContent = currentCount + 1;
                    }
                } catch (error) {
                    console.error('다운로드 카운터 업데이트 오류:', error);
                }
            }

            // 모달 외부 클릭 시 닫기
            document.getElementById('imagePreviewModal')?.addEventListener('click', function (e) {
                if (e.target === this) {
                    closeImagePreview();
                }
            });

            // 페이지 로드 시 초기화
            document.addEventListener('DOMContentLoaded', function () {
                loadNavigation();
                loadDownloadCounts(); // 다운로드 카운트 로드
            });

            // 다운로드 카운트 로드
            async function loadDownloadCounts() {
                try {
                    const attachmentElements = document.querySelectorAll('[data-attachment-id]');
                    console.log('다운로드 카운트 로드 시작:', attachmentElements.length + '개 파일');

                    for (const element of attachmentElements) {
                        const attachmentId = element.getAttribute('data-attachment-id');
                        const countElement = element.querySelector('.download-count');

                        if (countElement && attachmentId) {
                            try {
                                console.log(`첨부파일 ${attachmentId} 카운트 요청`);

                                // 다운로드 카운트 API 호출
                                const response = await fetch(`/api/board/attachments/${attachmentId}/count`);

                                if (response.ok) {
                                    const result = await response.json();
                                    console.log(`첨부파일 ${attachmentId} 카운트:`, result);

                                    if (result.success) {
                                        countElement.textContent = result.download_count || 0;
                                    } else {
                                        countElement.textContent = '0';
                                    }
                                } else {
                                    console.error(`첨부파일 ${attachmentId} 카운트 오류:`, response.status);
                                    countElement.textContent = '0';
                                }
                            } catch (fetchError) {
                                console.error(`첨부파일 ${attachmentId} 카운트 로드 오류:`, fetchError);
                                countElement.textContent = '0';
                            }
                        }
                    }

                    console.log('다운로드 카운트 로드 완료');

                } catch (error) {
                    console.error('다운로드 카운트 로드 오류:', error);
                }
            }
        </script>

        <%- include('../partials/footer') %>
</body>

</html>