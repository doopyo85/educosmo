<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Universe | EduCoding</title>

    <!-- CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <!-- Custom CSS for My Universe -->
    <style>
        .my-universe-nav {
            background-color: #fff;
            border-bottom: 1px solid #dee2e6;
            padding: 0 1rem;
        }

        .nav-link.active {
            font-weight: bold;
            color: #0d6efd !important;
            border-bottom: 2px solid #0d6efd;
            background: none !important;
        }

        .nav-link {
            color: #6c757d;
            padding: 1rem 1.5rem;
            border: none !important;
        }

        .nav-link:hover {
            color: #0d6efd;
            border: none;
        }
    </style>
</head>

<body>
    <%- include('../partials/header') %>

        <div class="board-layout-container">
            <!-- Sidebar -->
            <%- include('../partials/my-universe-sidebar', { activeTab: activeTab, student: locals.student }) %>

                <!-- Main Content (Right Side) -->
                <div class="board-main-content">
                    <% if (activeTab==='timeline' ) { %>
                        <%- include('./timeline') %>
                            <% } else if (activeTab==='gallery' ) { %>
                                <%- include('./gallery') %>
                                    <% } else if (activeTab==='observatory' ) { %>
                                        <%- include('./observatory') %>
                                            <% } else if (activeTab==='problems' ) { %>
                                                <%- include('./problems') %>
                                                    <% } else { %>
                                                        <div class="alert alert-info">준비 중인 기능입니다.</div>
                                                        <% } %>
                </div>
        </div>

        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>