<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Universe | EduCoding</title>

    <!-- CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <!-- Custom CSS for My Universe -->
    <style>
        .my-universe-nav {
            background-color: #fff;
            border-bottom: 1px solid #dee2e6;
            padding: 0 1rem;
        }

        .nav-link.active {
            font-weight: bold;
            color: #0d6efd !important;
            border-bottom: 2px solid #0d6efd;
            background: none !important;
        }

        .nav-link {
            color: #6c757d;
            padding: 1rem 1.5rem;
            border: none !important;
        }

        .nav-link:hover {
            color: #0d6efd;
            border: none;
        }
    </style>
</head>

<body>
    <%- include('../partials/header') %>

        <!-- My Universe Navigation -->
        <div class="my-universe-nav">
            <% // Determine base URL based on context (Student View vs Self View) let baseUrl='/my-universe' ; if
                (locals.student && student.id) { baseUrl=`/my-universe/student/${student.id}`; } %>
                <ul class="nav nav-tabs border-0">
                    <li class="nav-item">
                        <a class="nav-link <%= activeTab === 'timeline' ? 'active' : '' %>"
                            href="<%= baseUrl %><%= locals.student ? '' : '/timeline' %>">
                            <i class="bi bi-calendar-check me-2"></i>타임라인
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= activeTab === 'gallery' ? 'active' : '' %>" href="/gallery">
                            <i class="bi bi-grid me-2"></i>갤러리
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= activeTab === 'observatory' ? 'active' : '' %>"
                            href="<%= baseUrl %>/observatory">
                            <i class="bi bi-stars me-2"></i>Observatory
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">
                            <i class="bi bi-bar-chart me-2"></i>대시보드
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">
                            <i class="bi bi-journal-text me-2"></i>블로그
                        </a>
                    </li>
                </ul>
        </div>

        <!-- Content Area -->
        <div class="container-fluid py-3 px-4">
            <% if (activeTab==='timeline' ) { %>
                <%- include('./timeline') %>
                    <% } else if (activeTab==='observatory' ) { %>
                        <%- include('./observatory') %>
                            <% } else { %>
                                <div class="alert alert-info">준비 중인 기능입니다.</div>
                                <% } %>
        </div>

        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>