<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= blog.display_name %> - 교육용 보드
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        :root {
            --primary-bg: #f8f9fa;
            --sidebar-width: 280px;
            --card-shadow: 0 2px 12px rgba(0, 0, 0, 0.08);
            --header-height: 60px;
        }

        body {
            background-color: var(--primary-bg);
            font-family: 'Noto Sans KR', -apple-system, sans-serif;
            margin: 0;
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar Styles */
        .board-sidebar {
            width: var(--sidebar-width);
            background-color: white;
            border-right: 1px solid #eee;
            position: fixed;
            height: 100vh;
            left: 0;
            top: 0;
            z-index: 1000;
            display: flex;
            flex-direction: column;
            padding: 1.5rem;
        }

        .center-brand {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 2rem;
            color: #2c3e50;
            text-decoration: none;
        }

        .center-logo {
            width: 40px;
            height: 40px;
            border-radius: 8px;
            object-fit: cover;
        }

        .nav-section-title {
            font-size: 0.75rem;
            text-transform: uppercase;
            color: #888;
            font-weight: 700;
            margin-bottom: 0.8rem;
            letter-spacing: 0.5px;
        }

        .nav-item {
            display: flex;
            align-items: center;
            padding: 10px 12px;
            color: #555;
            text-decoration: none;
            border-radius: 8px;
            margin-bottom: 4px;
            transition: all 0.2s;
            font-size: 0.95rem;
        }

        .nav-item:hover {
            background-color: #f1f3f5;
            color: #2c3e50;
        }

        .nav-item.active {
            background-color: #e7f5ff;
            color: #007bff;
            font-weight: 600;
        }

        .nav-item i {
            margin-right: 12px;
            font-size: 1.1rem;
        }

        /* Main Content */
        .board-main {
            margin-left: var(--sidebar-width);
            flex: 1;
            padding: 2rem;
            max-width: 1400px;
        }

        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }

        .section-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: #2c3e50;
        }

        /* Card Grid */
        .post-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 1.5rem;
        }

        .post-card {
            background: white;
            border-radius: 12px;
            border: none;
            box-shadow: var(--card-shadow);
            overflow: hidden;
            transition: transform 0.2s, box-shadow 0.2s;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .post-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.12);
        }

        .card-img-top {
            height: 180px;
            object-fit: cover;
            border-bottom: 1px solid #f0f0f0;
        }

        .card-body {
            padding: 1.25rem;
            display: flex;
            flex-direction: column;
            flex: 1;
        }

        .card-title {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 0.8rem;
            line-height: 1.4;
        }

        .card-text {
            color: #666;
            font-size: 0.9rem;
            flex: 1;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .card-footer {
            background: white;
            border-top: 1px solid #f0f0f0;
            padding: 0.8rem 1.25rem;
            font-size: 0.85rem;
            color: #888;
            display: flex;
            justify-content: space-between;
        }

        /* Floating Write Button */
        .fab-write {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: #007bff;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 12px rgba(0, 123, 255, 0.4);
            cursor: pointer;
            transition: transform 0.2s;
            border: none;
            z-index: 999;
        }

        .fab-write:hover {
            transform: scale(1.1);
            background: #0056b3;
        }

        .empty-state {
            text-align: center;
            padding: 4rem 1rem;
            color: #999;
        }

        .empty-state i {
            font-size: 3rem;
            margin-bottom: 1rem;
            display: block;
        }
    </style>
</head>

<body>
    <!-- Sidebar -->
    <nav class="board-sidebar">
        <a href="/" class="center-brand">
            <% if (blog.logo_url) { %>
                <img src="<%= blog.logo_url %>" class="center-logo" alt="Logo">
                <% } else { %>
                    <div class="center-logo bg-light d-flex align-items-center justify-content-center">
                        <i class="bi bi-building"></i>
                    </div>
                    <% } %>
                        <div>
                            <div class="fw-bold">
                                <%= blog.display_name %>
                            </div>
                            <div class="small text-muted" style="font-size: 0.8rem;">교육 센터</div>
                        </div>
        </a>

        <div class="nav-section-title">Menu</div>
        <a href="/" class="nav-item active">
            <i class="bi bi-grid"></i> 홈
        </a>
        <a href="#" class="nav-item">
            <i class="bi bi-megaphone"></i> 공지사항
        </a>
        <a href="#" class="nav-item">
            <i class="bi bi-collection-play"></i> 학습 자료
        </a>
        <a href="#" class="nav-item">
            <i class="bi bi-images"></i> 갤러리
        </a>

        <div class="mt-auto">
            <div class="nav-section-title">User</div>
            <% if (user && user.is_logined) { %>
                <div class="d-flex align-items-center p-2 rounded bg-light mb-2">
                    <div class="flex-grow-1 ps-2">
                        <div class="fw-bold small">
                            <%= user.userID %>
                        </div>
                        <div class="text-muted" style="font-size: 11px;">
                            <%= user.role %>
                        </div>
                    </div>
                    <!-- <a href="/logout" class="btn btn-sm btn-link text-danger"><i class="bi bi-box-arrow-right"></i></a> -->
                </div>
                <% } else { %>
                    <a href="https://pong2.app/auth/login" class="btn btn-outline-primary w-100 btn-sm">
                        <i class="bi bi-person"></i> 로그인
                    </a>
                    <% } %>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="board-main">
        <div class="page-header">
            <h1 class="section-title">최신 포스트</h1>
            <!-- Search mock -->
            <div class="input-group" style="width: 300px;">
                <span class="input-group-text bg-white border-end-0"><i class="bi bi-search"></i></span>
                <input type="text" class="form-control border-start-0 ps-0" placeholder="검색...">
            </div>
        </div>

        <% if (posts.length===0) { %>
            <div class="empty-state">
                <i class="bi bi-journal-x"></i>
                <p>등록된 게시글이 없습니다.</p>
            </div>
            <% } else { %>
                <div class="post-grid">
                    <% posts.forEach(post=> { %>
                        <div class="post-card">
                            <a href="/p/<%= post.slug %>" class="text-decoration-none">
                                <% if (post.thumbnail_url) { %>
                                    <img src="<%= post.thumbnail_url %>" class="card-img-top" alt="<%= post.title %>">
                                    <% } else { %>
                                        <div
                                            class="card-img-top bg-light d-flex align-items-center justify-content-center text-muted">
                                            <i class="bi bi-image" style="font-size: 2rem;"></i>
                                        </div>
                                        <% } %>
                            </a>
                            <div class="card-body">
                                <a href="/p/<%= post.slug %>" class="text-decoration-none text-dark">
                                    <h5 class="card-title text-truncate">
                                        <%= post.title %>
                                    </h5>
                                </a>
                                <p class="card-text text-muted mb-2">
                                    <%= post.excerpt || '' %>
                                </p>
                            </div>
                            <div class="card-footer">
                                <span><i class="bi bi-calendar3 me-1"></i>
                                    <%= new Date(post.created_at).toLocaleDateString() %>
                                </span>
                                <span><i class="bi bi-eye"></i>
                                    <%= post.view_count %>
                                </span>
                            </div>
                        </div>
                        <% }); %>
                </div>

                <!-- Pagination -->
                <% if (pagination.totalPages> 1) { %>
                    <nav class="mt-5 d-flex justify-content-center">
                        <ul class="pagination">
                            <% for(let i=1; i <=pagination.totalPages; i++) { %>
                                <li class="page-item <%= pagination.page === i ? 'active' : '' %>">
                                    <a class="page-link" href="?page=<%= i %>">
                                        <%= i %>
                                    </a>
                                </li>
                                <% } %>
                        </ul>
                    </nav>
                    <% } %>
                        <% } %>
    </main>

    <!-- Write Button (Only for Admins/Teachers) -->
    <% if (user && (user.role==='admin' || user.role==='manager' || user.role==='teacher' )) { %>
        <a href="/write" class="fab-write" title="글쓰기">
            <i class="bi bi-pencil-fill" style="font-size: 1.5rem;"></i>
        </a>
        <% } %>

            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>