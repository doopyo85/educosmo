<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- SEO Meta Tags -->
    <title><%= seoData?.title || `${post.title} - ${blog.title}` %></title>
    <meta name="description" content="<%= seoData?.description || post.excerpt || '' %>">
    <meta name="keywords" content="코딩, 프로그래밍, <%= owner.name %>, MyUniverse">
    <meta name="author" content="<%= owner.name %>">

    <!-- Open Graph -->
    <meta property="og:title" content="<%= seoData?.title || post.title %>">
    <meta property="og:description" content="<%= seoData?.description || post.excerpt || '' %>">
    <meta property="og:image" content="<%= seoData?.ogImage || '' %>">
    <meta property="og:url" content="<%= seoData?.url || '' %>">
    <meta property="og:type" content="article">
    <meta property="og:site_name" content="MyUniverse">
    <% if (seoData?.publishedTime) { %>
        <meta property="article:published_time" content="<%= new Date(seoData.publishedTime).toISOString() %>">
    <% } %>
    <% if (seoData?.modifiedTime) { %>
        <meta property="article:modified_time" content="<%= new Date(seoData.modifiedTime).toISOString() %>">
    <% } %>

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= seoData?.title || post.title %>">
    <meta name="twitter:description" content="<%= seoData?.description || post.excerpt || '' %>">
    <meta name="twitter:image" content="<%= seoData?.ogImage || '' %>">

    <!-- Additional SEO -->
    <link rel="canonical" href="<%= seoData?.url || '' %>">
    <meta name="robots" content="index, follow">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/vs2015.min.css">
    <link rel="stylesheet" href="/css/tiptap-notion.css">

    <style>
        :root {
            --bg-color: #191919;
            --sidebar-color: #202020;
            --text-color: #d3d3d3;
            --hover-color: #2c2c2c;
            --accent-color: #e0e0e0;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, "Apple Color Emoji", Arial, sans-serif;
            margin: 0;
            overflow-x: hidden;
        }

        .layout {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar - Same as home */
        .sidebar {
            width: 250px;
            background-color: var(--sidebar-color);
            padding: 20px;
            display: flex;
            flex-direction: column;
            border-right: 1px solid #333;
            flex-shrink: 0;
        }

        .profile-section {
            display: flex;
            flex-direction: column;
            gap: 16px;
            margin-bottom: 30px;
            padding: 16px;
            border-radius: 8px;
            background-color: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .profile-header {
            display: flex;
            align-items: center;
            gap: 12px;
            cursor: pointer;
            padding: 4px;
            border-radius: 6px;
            transition: background 0.2s;
        }

        .profile-header:hover {
            background-color: var(--hover-color);
        }

        .profile-img {
            width: 40px;
            height: 40px;
            border-radius: 6px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            object-fit: cover;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 18px;
            font-weight: 600;
        }

        .profile-info {
            flex: 1;
            min-width: 0;
        }

        .blog-title {
            font-weight: 600;
            font-size: 14px;
            color: var(--accent-color);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            margin-bottom: 2px;
        }

        .blog-owner {
            font-size: 12px;
            color: #888;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 10px;
            color: #999;
            text-decoration: none;
            border-radius: 4px;
            margin-bottom: 2px;
            font-size: 14px;
            transition: all 0.2s;
        }

        .nav-item:hover {
            background-color: var(--hover-color);
            color: var(--text-color);
        }

        /* Main Content */
        .main-content {
            flex-grow: 1;
            padding: 60px 100px;
            max-width: 900px;
            margin: 0 auto;
        }

        /* Post Header */
        .post-header {
            margin-bottom: 40px;
            padding-bottom: 30px;
            border-bottom: 1px solid #2a2a2a;
        }

        .post-title {
            font-size: 40px;
            font-weight: 700;
            color: var(--accent-color);
            margin-bottom: 16px;
            line-height: 1.2;
        }

        .post-meta {
            display: flex;
            gap: 20px;
            font-size: 14px;
            color: #888;
        }

        .post-meta-item {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .author-section {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 16px;
            background-color: rgba(255, 255, 255, 0.03);
            border-radius: 8px;
            margin-bottom: 30px;
        }

        .author-img {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 20px;
            font-weight: 600;
        }

        .author-info {
            flex: 1;
        }

        .author-name {
            font-weight: 600;
            color: white;
            margin-bottom: 2px;
        }

        .author-date {
            font-size: 13px;
            color: #888;
        }

        /* Post Content - Use Tiptap Notion styles */
        .post-content {
            margin-bottom: 60px;
        }

        /* Back button */
        .back-btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            color: #888;
            text-decoration: none;
            border-radius: 6px;
            transition: all 0.2s;
            margin-bottom: 20px;
        }

        .back-btn:hover {
            background-color: rgba(255, 255, 255, 0.05);
            color: white;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                display: none;
            }

            .main-content {
                padding: 40px 20px;
            }

            .post-title {
                font-size: 28px;
            }
        }
    </style>
</head>

<body>
    <div class="layout">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="profile-section">
                <div class="profile-header" onclick="location.href='./'">
                    <% if(owner && owner.profile_image) { %>
                        <img src="<%= owner.profile_image %>" class="profile-img" alt="<%= owner.name %>">
                    <% } else { %>
                        <div class="profile-img">
                            <%= owner.name ? owner.name.charAt(0).toUpperCase() : 'U' %>
                        </div>
                    <% } %>
                    <div class="profile-info">
                        <div class="blog-title"><%= blog.title %></div>
                        <div class="blog-owner"><%= owner.name %>'s blog</div>
                    </div>
                </div>
            </div>

            <nav>
                <a href="../" class="nav-item">
                    <i class="fa-solid fa-house"></i> 홈 (Home)
                </a>

                <% /* Check if owner */
                let isOwner = false;
                let visitorId = null;
                if (user) {
                    if (user.id) visitorId = user.id;
                    else if (user.user && user.user.id) visitorId = user.user.id;
                    else if (user.userID) visitorId = user.userID;
                    else if (user.dbId) visitorId = user.dbId;
                }
                if (visitorId && owner && String(visitorId) === String(owner.id)) {
                    isOwner = true;
                }
                %>

                <% if(isOwner) { %>
                    <div class="mt-3">
                        <div class="text-secondary small px-2 mb-2" style="font-size: 11px; font-weight: 600;">기능 (ACTIONS)</div>
                        <a href="../write" class="nav-item">
                            <i class="fa-solid fa-pen-to-square"></i> 글쓰기
                        </a>
                    </div>
                <% } %>
            </nav>
        </aside>

        <!-- Main -->
        <main class="main-content">
            <a href="../" class="back-btn">
                <i class="fa-solid fa-arrow-left"></i>
                <span>뒤로가기</span>
            </a>

            <article class="post">
                <div class="post-header">
                    <h1 class="post-title"><%= post.title %></h1>

                    <div class="author-section">
                        <% if(owner && owner.profile_image) { %>
                            <img src="<%= owner.profile_image %>" class="author-img" alt="<%= owner.name %>">
                        <% } else { %>
                            <div class="author-img">
                                <%= owner.name ? owner.name.charAt(0).toUpperCase() : 'U' %>
                            </div>
                        <% } %>
                        <div class="author-info">
                            <div class="author-name"><%= owner.name %></div>
                            <div class="author-date">
                                <%= new Date(post.created_at).toLocaleDateString('ko-KR', { year: 'numeric', month: 'long', day: 'numeric' }) %>
                            </div>
                        </div>
                    </div>

                    <div class="post-meta">
                        <span class="post-meta-item">
                            <i class="fa-regular fa-calendar"></i>
                            <%= new Date(post.created_at).toLocaleDateString('ko-KR') %>
                        </span>
                        <span class="post-meta-item">
                            <i class="fa-regular fa-eye"></i>
                            <%= post.view_count %> views
                        </span>
                        <% if(post.updated_at && post.updated_at !== post.created_at) { %>
                            <span class="post-meta-item">
                                <i class="fa-regular fa-clock"></i>
                                Updated <%= new Date(post.updated_at).toLocaleDateString('ko-KR') %>
                            </span>
                        <% } %>
                    </div>
                </div>

                <div class="post-content notion-editor-content">
                    <%- post.content || '' %>
                </div>
            </article>
        </main>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
    <script>
        // Highlight code blocks
        document.addEventListener('DOMContentLoaded', () => {
            document.querySelectorAll('pre code').forEach((block) => {
                hljs.highlightElement(block);
            });
        });
    </script>
</body>

</html>
