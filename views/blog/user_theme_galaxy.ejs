<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Basic Meta Tags -->
    <title>
        <%= seoData?.title || blog.title %>
    </title>
    <meta name="description" content="<%= seoData?.description || blog.description || '' %>">
    <meta name="keywords" content="코딩, 프로그래밍, 블로그, <%= owner.name %>, MyUniverse">
    <meta name="author" content="<%= owner.name %>">

    <!-- Open Graph (Facebook, LinkedIn) -->
    <meta property="og:title" content="<%= seoData?.title || blog.title %>">
    <meta property="og:description" content="<%= seoData?.description || blog.description || '' %>">
    <meta property="og:image" content="<%= seoData?.ogImage || '' %>">
    <meta property="og:url" content="<%= seoData?.url || '' %>">
    <meta property="og:type" content="<%= seoData?.type || 'website' %>">
    <meta property="og:site_name" content="MyUniverse">

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= seoData?.title || blog.title %>">
    <meta name="twitter:description" content="<%= seoData?.description || blog.description || '' %>">
    <meta name="twitter:image" content="<%= seoData?.ogImage || '' %>">

    <!-- Additional SEO -->
    <link rel="canonical" href="<%= seoData?.url || '' %>">
    <meta name="robots" content="index, follow">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --bg-color: #191919;
            --sidebar-color: #202020;
            --text-color: #d3d3d3;
            --hover-color: #2c2c2c;
            --accent-color: #e0e0e0;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, "Apple Color Emoji", Arial, sans-serif;
            margin: 0;
            overflow-x: hidden;
        }

        .layout {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            width: 250px;
            background-color: var(--sidebar-color);
            padding: 20px;
            display: flex;
            flex-direction: column;
            border-right: 1px solid #333;
            flex-shrink: 0;
        }

        .profile-section {
            display: flex;
            flex-direction: column;
            gap: 16px;
            margin-bottom: 30px;
            padding: 16px;
            border-radius: 8px;
            background-color: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .profile-header {
            display: flex;
            align-items: center;
            gap: 12px;
            cursor: pointer;
            padding: 4px;
            border-radius: 6px;
            transition: background 0.2s;
        }

        .profile-header:hover {
            background-color: var(--hover-color);
        }

        .profile-img {
            width: 40px;
            height: 40px;
            border-radius: 6px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            object-fit: cover;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 18px;
            font-weight: 600;
        }

        .profile-info {
            flex: 1;
            min-width: 0;
        }

        .blog-title {
            font-weight: 600;
            font-size: 14px;
            color: var(--accent-color);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            margin-bottom: 2px;
        }

        .blog-owner {
            font-size: 12px;
            color: #888;
        }

        .profile-description {
            font-size: 12px;
            color: #999;
            line-height: 1.5;
            padding: 8px 0;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 10px;
            color: #999;
            text-decoration: none;
            border-radius: 4px;
            margin-bottom: 2px;
            font-size: 14px;
            transition: all 0.2s;
        }

        .nav-item:hover {
            background-color: var(--hover-color);
            color: var(--text-color);
        }

        .nav-item.active {
            background-color: var(--hover-color);
            color: var(--text-color);
            font-weight: 500;
        }

        /* Main Content */
        .main-content {
            flex-grow: 1;
            padding: 60px 100px;
            max-width: 900px;
            /* Notion-like width constraint */
            margin: 0 auto;
        }

        /* Post List */
        .page-header {
            margin-bottom: 40px;
        }

        .page-icon {
            font-size: 40px;
            margin-bottom: 20px;
        }

        .page-title {
            font-size: 40px;
            font-weight: 700;
            color: var(--accent-color);
            margin-bottom: 10px;
        }

        .post-item {
            display: block;
            padding: 16px;
            margin-bottom: 8px;
            border-radius: 6px;
            text-decoration: none;
            color: var(--text-color);
            transition: all 0.2s;
            border: 1px solid transparent;
            background-color: rgba(255, 255, 255, 0.02);
        }

        .post-item:hover {
            background-color: rgba(255, 255, 255, 0.05);
            border-color: rgba(255, 255, 255, 0.1);
            color: white;
            transform: translateX(4px);
        }

        .post-title {
            font-size: 16px;
            font-weight: 500;
            margin-bottom: 6px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .post-icon {
            font-size: 14px;
            color: #888;
        }

        .post-meta {
            font-size: 12px;
            color: #666;
            display: flex;
            gap: 12px;
            align-items: center;
        }

        .post-excerpt {
            font-size: 13px;
            color: #888;
            margin-top: 6px;
            line-height: 1.5;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                display: none;
            }

            .main-content {
                padding: 40px 20px;
            }
        }
    </style>
</head>

<body>
    <div class="layout">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="profile-section">
                <div class="profile-header" onclick="location.href='./'">
                    <% if(owner && owner.profile_image) { %>
                        <img src="<%= owner.profile_image %>" class="profile-img" alt="<%= owner.name %>">
                    <% } else { %>
                        <div class="profile-img">
                            <%= owner.name ? owner.name.charAt(0).toUpperCase() : 'U' %>
                        </div>
                    <% } %>
                    <div class="profile-info">
                        <div class="blog-title"><%= blog.title %></div>
                        <div class="blog-owner"><%= owner.name %>'s blog</div>
                    </div>
                </div>
                <% if(blog.description) { %>
                    <div class="profile-description"><%= blog.description %></div>
                <% } %>
            </div>

            <nav>
                <a href="./" class="nav-item active">
                    <i class="fa-solid fa-house"></i> 홈 (Home)
                </a>

                <% /* DEBUG: Write Button Logic */ let isOwner=false; let visitorId=null; if (user) { if (user.id)
                    visitorId=user.id; else if (user.user && user.user.id) visitorId=user.user.id; else if (user.userID)
                    visitorId=user.userID; else if (user.dbId) visitorId=user.dbId; } if (visitorId && owner &&
                    String(visitorId)===String(owner.id)) { isOwner=true; } %>

                    <% if(isOwner) { %>
                        <div class="mt-3">
                            <div class="text-secondary small px-2 mb-2" style="font-size: 11px; font-weight: 600;">기능
                                (ACTIONS)</div>
                            <a href="write" class="nav-item">
                                <i class="fa-solid fa-pen-to-square"></i> 글쓰기
                            </a>
                        </div>
                        <% } %>
            </nav>
        </aside>

        <!-- Main -->
        <main class="main-content">
            <div class="page-header">
                <div class="page-icon">🌌</div>
                <h1 class="page-title">
                    <%= blog.title %>
                </h1>
                <p class="text-secondary">
                    <%= blog.description || ' 환영합니다.' %>
                </p>
            </div>

            <div class="post-list">
                <% if (posts.length===0) { %>
                    <div style="padding: 60px 20px; text-align: center;">
                        <div style="font-size: 48px; margin-bottom: 16px;">📝</div>
                        <p class="text-muted">아직 작성된 글이 없습니다.</p>
                        <% if(isOwner) { %>
                            <a href="write" class="btn btn-sm btn-outline-light mt-3">
                                <i class="fa-solid fa-pen-to-square me-2"></i>첫 글 작성하기
                            </a>
                        <% } %>
                    </div>
                <% } else { %>
                    <% posts.forEach(post => { %>
                        <a href="p/<%= post.slug %>" class="post-item">
                            <div class="post-title">
                                <% if(post.thumbnail_url) { %>
                                    <i class="fa-regular fa-image post-icon"></i>
                                <% } else if(post.file_type === 'code') { %>
                                    <i class="fa-solid fa-code post-icon"></i>
                                <% } else { %>
                                    <i class="fa-regular fa-file-lines post-icon"></i>
                                <% } %>
                                <span><%= post.title %></span>
                            </div>
                            <div class="post-meta">
                                <span><i class="fa-regular fa-calendar me-1"></i><%= new Date(post.created_at).toLocaleDateString('ko-KR') %></span>
                                <% if(post.view_count > 0) { %>
                                    <span><i class="fa-regular fa-eye me-1"></i><%= post.view_count %></span>
                                <% } %>
                            </div>
                            <% if(post.excerpt) { %>
                                <div class="post-excerpt"><%= post.excerpt.substring(0, 120) %><%= post.excerpt.length > 120 ? '...' : '' %></div>
                            <% } %>
                        </a>
                    <% }); %>
                <% } %>

                                    <!-- Pagination -->
                                    <% if (pagination.totalPages> 1) { %>
                                        <div class="mt-4">
                                            <% for(let i=1; i <=pagination.totalPages; i++) { %>
                                                <a href="?page=<%= i %>"
                                                    class="btn btn-sm btn-outline-secondary <%= pagination.page === i ? 'active' : '' %>">
                                                    <%= i %>
                                                </a>
                                                <% } %>
                                        </div>
                                        <% } %>
            </div>
        </main>
    </div>
</body>

</html>