<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= blog.title %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --bg-color: #191919;
            --sidebar-color: #202020;
            --text-color: #d3d3d3;
            --hover-color: #2c2c2c;
            --accent-color: #e0e0e0;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, "Apple Color Emoji", Arial, sans-serif;
            margin: 0;
            overflow-x: hidden;
        }

        .layout {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            width: 250px;
            background-color: var(--sidebar-color);
            padding: 20px;
            display: flex;
            flex-direction: column;
            border-right: 1px solid #333;
            flex-shrink: 0;
        }

        .profile-section {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 30px;
            padding: 10px;
            border-radius: 6px;
            cursor: pointer;
            transition: background 0.2s;
        }

        .profile-section:hover {
            background-color: var(--hover-color);
        }

        .profile-img {
            width: 30px;
            height: 30px;
            border-radius: 4px;
            background-color: #444;
            /* Fallback */
            object-fit: cover;
        }

        .blog-title {
            font-weight: 600;
            font-size: 14px;
            color: var(--accent-color);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 10px;
            color: #999;
            text-decoration: none;
            border-radius: 4px;
            margin-bottom: 2px;
            font-size: 14px;
            transition: all 0.2s;
        }

        .nav-item:hover {
            background-color: var(--hover-color);
            color: var(--text-color);
        }

        .nav-item.active {
            background-color: var(--hover-color);
            color: var(--text-color);
            font-weight: 500;
        }

        /* Main Content */
        .main-content {
            flex-grow: 1;
            padding: 60px 100px;
            max-width: 900px;
            /* Notion-like width constraint */
            margin: 0 auto;
        }

        /* Post List */
        .page-header {
            margin-bottom: 40px;
        }

        .page-icon {
            font-size: 40px;
            margin-bottom: 20px;
        }

        .page-title {
            font-size: 40px;
            font-weight: 700;
            color: var(--accent-color);
            margin-bottom: 10px;
        }

        .post-item {
            display: block;
            padding: 12px 0;
            border-bottom: 1px solid #2a2a2a;
            text-decoration: none;
            color: var(--text-color);
            transition: padding-left 0.2s;
        }

        .post-item:hover {
            padding-left: 10px;
            color: white;
        }

        .post-title {
            font-size: 16px;
            font-weight: 500;
            margin-bottom: 4px;
        }

        .post-meta {
            font-size: 12px;
            color: #666;
            display: flex;
            gap: 15px;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                display: none;
            }

            .main-content {
                padding: 40px 20px;
            }
        }
    </style>
</head>

<body>
    <div class="layout">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="profile-section" onclick="location.href='./'">
                <% if(owner && owner.profile_image) { %>
                    <img src="<%= owner.profile_image %>" class="profile-img" alt="Profile">
                    <% } else { %>
                        <div class="profile-img d-flex align-items-center justify-content-center">
                            <i class="fa-solid fa-user text-white"></i>
                        </div>
                        <% } %>
                            <div class="blog-title">
                                <%= blog.title %>
                            </div>
            </div>

            <nav>
                <a href="./" class="nav-item active">
                    <i class="fa-solid fa-house"></i> 홈 (Home)
                </a>

                <% // Check Owner for Write Button let isOwner=false; if (user && user.user) { // Internal access via
                    /blog proxies session weirdly sometimes, check structure // Adjust based on your actual session
                    structure logged in server-blog.js isOwner=(user.user.id==owner.id) || (user.id==owner.id); } else
                    if (user && user.id) { isOwner=(user.id==owner.id); } %>

                    <% if(isOwner) { %>
                        <div class="mt-3">
                            <div class="text-secondary small px-2 mb-2" style="font-size: 11px; font-weight: 600;">기능
                                (ACTIONS)</div>
                            <a href="write" class="nav-item">
                                <i class="fa-solid fa-pen-to-square"></i> 글쓰기
                            </a>
                        </div>
                        <% } %>
            </nav>
        </aside>

        <!-- Main -->
        <main class="main-content">
            <div class="page-header">
                <div class="page-icon">🌌</div>
                <h1 class="page-title">
                    <%= blog.title %>
                </h1>
                <p class="text-secondary">
                    <%= blog.description || '환영합니다.' %>
                </p>
            </div>

            <div class="post-list">
                <% if (posts.length===0) { %>
                    <p class="text-muted">아직 작성된 글이 없습니다. 📝</p>
                    <% } else { %>
                        <% posts.forEach(post=> { %>
                            <a href="p/<%= post.slug %>" class="post-item">
                                <div class="post-title">
                                    <% if(post.thumbnail_url) { %>
                                        <i class="fa-regular fa-image me-2"></i>
                                        <% } else { %>
                                            <i class="fa-regular fa-file-lines me-2"></i>
                                            <% } %>
                                                <%= post.title %>
                                </div>
                                <div class="post-meta">
                                    <span>
                                        <%= new Date(post.created_at).toLocaleDateString() %>
                                    </span>
                                    <% if(post.excerpt) { %>
                                        <span>
                                            <%= post.excerpt.substring(0, 30) %>...
                                        </span>
                                        <% } %>
                                </div>
                            </a>
                            <% }); %>
                                <% } %>

                                    <!-- Pagination -->
                                    <% if (pagination.totalPages> 1) { %>
                                        <div class="mt-4">
                                            <% for(let i=1; i <=pagination.totalPages; i++) { %>
                                                <a href="?page=<%= i %>"
                                                    class="btn btn-sm btn-outline-secondary <%= pagination.page === i ? 'active' : '' %>">
                                                    <%= i %>
                                                </a>
                                                <% } %>
                                        </div>
                                        <% } %>
            </div>
        </main>
    </div>
</body>

</html>