================================================================================
COMPONENT-SYSTEM 세부 문서
================================================================================
시스템 이름: Component-System (컴포넌트 기반 학습 시스템)
적용 대상: Python, Algorithm, Certification 학습 페이지
버전: 1.0

================================================================================
시스템 개요
================================================================================

Component-System은 고도로 모듈화된 컴포넌트 기반 학습 시스템입니다.
학습자가 다양한 학습 도구(IDE, Quiz, Content, Jupyter 등)를 동적으로 전환하며
사용할 수 있도록 설계되었습니다.

주요 특징:
  ✓ 동적 컴포넌트 로딩 시스템
  ✓ IDE (Python 코드 편집기) 통합
  ✓ Quiz (퀴즈 평가) 시스템
  ✓ Jupyter Notebook 임베딩
  ✓ PPT Viewer (프레젠테이션 뷰어)
  ✓ CSS 변수 기반 반응형 레이아웃

================================================================================
적용 라우트
================================================================================

/python_project     → template.ejs (pageType: 'pythontest')
/algorithm          → template.ejs (pageType: 'algorithm')
/certification      → template.ejs (pageType: 'algorithm')
/pythontest         → template.ejs (pageType: 'pythontest')

모든 라우트가 동일한 template.ejs를 사용하며, pageType 파라미터로 구분됩니다.

================================================================================
파일 구조
================================================================================

View Files:
  /views/template.ejs                        ← 메인 템플릿 파일

CSS Files (Core Layout):
  /public/css/common-layout.css              ← 핵심 레이아웃 구조
  /public/css/common-content.css             ← 콘텐츠 디스플레이
  /public/css/common-ide.css                 ← IDE 스타일링
  /public/css/common-quiz.css                ← Quiz 스타일링
  /public/css/ppt-viewer.css                 ← PPT 뷰어 스타일

CSS Files (Components):
  /public/css/components/navigation.css      ← 네비게이션 컴포넌트
  /public/css/components/content.css         ← 콘텐츠 컴포넌트
  /public/css/components/explanation.css     ← 설명 팝업
  /public/css/components/layout-toggle.css   ← 레이아웃 토글 컨트롤
  /public/css/components/jupyter.css         ← Jupyter 통합

CSS Files (IDE Specific):
  /public/css/ide/ide-core.css               ← IDE 핵심 기능
  /public/css/ide/ide-editor.css             ← 코드 에디터 UI
  /public/css/ide/ide-multifile.css          ← 멀티 파일 지원
  /public/css/ide/ide-terminal.css           ← 터미널 출력

JavaScript Files:
  /public/js/component-system.js             ← 컴포넌트 시스템 오케스트레이터
  /public/js/components/base/ComponentRegistry.js  ← 컴포넌트 레지스트리
  /public/js/components/nav/NavigationComponent.js ← 네비게이션
  /public/js/components/content/ContentComponent.js ← 콘텐츠
  /public/js/components/ide/IDEComponent.js  ← IDE
  /public/js/components/quiz/QuizComponent.js ← Quiz
  /public/js/components/jupyter/JupyterComponent.js ← Jupyter

================================================================================
HTML 레이아웃 구조
================================================================================

<body>
  <%- include('partials/header') %>

  <!-- Main Container -->
  <div class="mainContainer">

    <!-- Left Navigation Sidebar (260px fixed) -->
    <nav class="nav-sidebar">
      <div class="nav-header">...</div>
      <div class="nav-items">...</div>
    </nav>

    <!-- Main Area (flexible) -->
    <div class="mainArea">

      <!-- Layout Modes (동적으로 전환) -->

      <!-- 1. HTML Content Layout -->
      <div class="layout-html">
        <div id="content-component"></div>
      </div>

      <!-- 2. IDE Layout -->
      <div class="layout-ide">
        <div id="content-component"></div>
        <div id="ide-component"></div>
      </div>

      <!-- 3. Quiz Layout -->
      <div class="layout-quiz">
        <div id="quiz-component"></div>
      </div>

      <!-- 4. Jupyter Layout -->
      <div class="layout-jupyter">
        <div id="jupyter-component"></div>
      </div>

    </div>

  </div>
</body>

================================================================================
CSS 변수 시스템
================================================================================

Component-System은 CSS 변수를 통해 동적으로 레이아웃을 제어합니다.

주요 CSS 변수:

--content-width: 50%      ← Content 영역 너비
--ide-width: 50%          ← IDE 영역 너비
--nav-width: 260px        ← 네비게이션 사이드바 너비

사용 예시:

/* Content와 IDE를 50:50으로 분할 */
.layout-ide {
  display: grid;
  grid-template-columns: var(--content-width) var(--ide-width);
}

/* 동적 조정 (JavaScript) */
document.documentElement.style.setProperty('--content-width', '70%');
document.documentElement.style.setProperty('--ide-width', '30%');

================================================================================
컴포넌트 시스템 아키텍처
================================================================================

┌──────────────────────────────────────────────────────────────┐
│                     ComponentRegistry                         │
│  (모든 컴포넌트를 등록하고 관리하는 중앙 레지스트리)          │
└──────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
┌───────▼────────┐  ┌─────────▼────────┐  ┌────────▼─────────┐
│ NavigationComp │  │   ContentComp    │  │    IDEComp       │
│ - 학습 메뉴    │  │   - HTML 콘텐츠  │  │    - 코드 에디터 │
│ - 진도 표시    │  │   - 설명 팝업    │  │    - 실행 버튼   │
└────────────────┘  └──────────────────┘  └──────────────────┘
        │                     │                     │
┌───────▼────────┐  ┌─────────▼────────┐  ┌────────▼─────────┐
│   QuizComp     │  │  JupyterComp     │  │   (확장 가능)    │
│ - 문제 표시    │  │  - Notebook 임베드│  │                  │
│ - 답안 제출    │  │  - 실행 결과     │  │                  │
└────────────────┘  └──────────────────┘  └──────────────────┘

각 컴포넌트는 독립적으로 로드/언로드 가능하며,
ComponentRegistry가 생명주기를 관리합니다.

================================================================================
레이아웃 모드
================================================================================

Component-System은 4가지 레이아웃 모드를 지원합니다:

1. layout-html (HTML Content Only)
   - Content Component만 표시
   - 전체 너비 사용
   - 사용: 이론 학습, 설명 페이지

2. layout-ide (Content + IDE Split)
   - Content Component (좌측) + IDE Component (우측)
   - CSS 변수로 비율 조정 가능
   - 사용: Python 코딩 실습

3. layout-quiz (Quiz Full Screen)
   - Quiz Component 전체 화면 표시
   - 사용: 문제 풀이, 평가

4. layout-jupyter (Jupyter Notebook)
   - Jupyter Component 전체 화면 표시
   - 사용: Jupyter Notebook 실습

레이아웃 전환은 JavaScript로 자동 처리:

// NavigationComponent에서 항목 클릭 시
componentRegistry.setLayout('layout-ide');

================================================================================
컴포넌트 가시성 제어
================================================================================

각 컴포넌트는 .component-visible / .component-hidden 클래스로 제어됩니다.

CSS:
.component-hidden {
  display: none !important;
}

.component-visible {
  display: block;
}

JavaScript 제어:
componentRegistry.showComponent('content');
componentRegistry.hideComponent('ide');

================================================================================
반응형 디자인
================================================================================

Breakpoints:

@media (max-width: 1200px) {
  /* Tablet: 네비게이션 너비 축소 */
  --nav-width: 200px;
}

@media (max-width: 992px) {
  /* Mobile: 세로 스택 레이아웃 */
  .layout-ide {
    grid-template-columns: 1fr;
    grid-template-rows: auto auto;
  }
}

@media (max-width: 768px) {
  /* Small Mobile: 네비게이션 숨김 (토글 메뉴) */
  .nav-sidebar {
    position: fixed;
    left: -260px;
    transition: left 0.3s;
  }

  .nav-sidebar.active {
    left: 0;
  }
}

================================================================================
IDE 컴포넌트 통합
================================================================================

IDE 컴포넌트는 다음 기능을 제공합니다:

1. 코드 에디터 (Monaco Editor 기반)
   - 구문 강조 (Syntax Highlighting)
   - 자동 완성 (Auto-completion)
   - 에러 표시 (Error Highlighting)

2. 파일 관리
   - 멀티 파일 지원 (탭 방식)
   - 파일 추가/삭제
   - 파일 전환

3. 실행 버튼
   - 코드 실행 (서버로 전송)
   - 실행 결과 표시 (터미널)
   - 에러 처리

HTML 구조:
<div id="ide-component">
  <div class="ide-toolbar">
    <button class="run-button">실행</button>
  </div>
  <div class="ide-editor-container">
    <div id="monaco-editor"></div>
  </div>
  <div class="ide-terminal">
    <pre id="output"></pre>
  </div>
</div>

================================================================================
Quiz 컴포넌트 통합
================================================================================

Quiz 컴포넌트는 문제 풀이 및 평가 기능을 제공합니다:

1. 문제 표시
   - 객관식 (Multiple Choice)
   - 주관식 (Short Answer)
   - 코드 작성 (Code Writing)

2. 답안 제출
   - 실시간 피드백
   - 정답/오답 표시
   - 점수 계산

3. 진도 추적
   - 완료된 문제 표시
   - 전체 진도율 계산

HTML 구조:
<div id="quiz-component">
  <div class="quiz-question">
    <h3>문제 1</h3>
    <p>질문 내용...</p>
    <div class="quiz-options">
      <label><input type="radio" name="q1" value="1"> 선택지 1</label>
      <label><input type="radio" name="q1" value="2"> 선택지 2</label>
    </div>
  </div>
  <button class="submit-quiz">제출</button>
</div>

================================================================================
Jupyter 컴포넌트 통합
================================================================================

Jupyter 컴포넌트는 Jupyter Notebook을 임베드합니다:

1. Notebook 로딩
   - .ipynb 파일을 JSON으로 파싱
   - 셀 단위로 렌더링

2. 실행 지원
   - 코드 셀 실행
   - 결과 표시 (텍스트, 이미지, 그래프)

3. 인터랙티브 편집
   - 셀 추가/삭제
   - 마크다운 편집

HTML 구조:
<div id="jupyter-component">
  <div class="jupyter-cells">
    <div class="jupyter-cell code-cell">
      <div class="cell-input">...</div>
      <div class="cell-output">...</div>
    </div>
  </div>
</div>

================================================================================
Navigation 컴포넌트
================================================================================

Navigation 컴포넌트는 학습 목차를 표시하고 진도를 추적합니다:

구조:
<nav class="nav-sidebar">
  <div class="nav-header">
    <h2>Python 기초</h2>
  </div>
  <div class="nav-items">
    <div class="nav-item" data-lesson-id="1">
      <span class="nav-icon">📘</span>
      <span class="nav-title">1. 변수와 자료형</span>
      <span class="progress-badge">완료</span>
    </div>
    <div class="nav-item active" data-lesson-id="2">
      <span class="nav-icon">📗</span>
      <span class="nav-title">2. 조건문</span>
      <span class="progress-badge">진행중</span>
    </div>
  </div>
</nav>

상태 표시:
- .nav-item.completed   → 완료됨 (체크 아이콘)
- .nav-item.active      → 현재 학습 중 (하이라이트)
- .nav-item.locked      → 잠금 (이전 단계 미완료)

================================================================================
수정 시 주의사항
================================================================================

⚠️ 절대 수정 금지:
  - common-layout.css의 기본 구조 (mainContainer, nav-sidebar, mainArea)
  - CSS 변수 이름 (--content-width, --ide-width, --nav-width)
  - ComponentRegistry API (컴포넌트들이 의존)

⚠️ 수정 시 테스트 필수:
  - IDE 레이아웃 (Content + IDE 분할)
  - Quiz 레이아웃 (전체 화면)
  - Jupyter 레이아웃
  - 반응형 동작 (모바일, 태블릿)

⚠️ 다른 시스템에 영향:
  - styles.css 수정 시 Card-Layout, Board-System 확인
  - header.css 수정 시 모든 시스템 확인

================================================================================
새 컴포넌트 추가 방법
================================================================================

1. JavaScript 컴포넌트 클래스 생성
   /public/js/components/mycomponent/MyComponent.js

   class MyComponent {
     constructor(registry) {
       this.registry = registry;
       this.element = document.getElementById('my-component');
     }

     load() { /* 초기화 */ }
     unload() { /* 정리 */ }
   }

2. CSS 파일 생성
   /public/css/components/mycomponent.css

3. ComponentRegistry에 등록
   // component-system.js
   import MyComponent from './components/mycomponent/MyComponent.js';

   registry.register('mycomponent', new MyComponent(registry));

4. template.ejs에 HTML 추가
   <div id="my-component" class="component-hidden"></div>

5. CSS 임포트
   <link rel="stylesheet" href="/css/components/mycomponent.css">

================================================================================
디버깅 팁
================================================================================

1. 컴포넌트 로딩 확인
   console.log(componentRegistry.components);

2. 현재 레이아웃 확인
   console.log(componentRegistry.currentLayout);

3. CSS 변수 확인
   getComputedStyle(document.documentElement).getPropertyValue('--content-width');

4. 컴포넌트 강제 표시
   document.getElementById('ide-component').classList.add('component-visible');

================================================================================
성능 최적화
================================================================================

1. 레이지 로딩
   - 컴포넌트는 필요할 때만 로드
   - ComponentRegistry.load() 호출 시 로드

2. 메모리 관리
   - 컴포넌트 전환 시 이전 컴포넌트 unload()
   - 이벤트 리스너 정리

3. CSS 최적화
   - GPU 가속 사용 (transform, opacity)
   - Reflow 최소화 (CSS 변수 사용)

================================================================================
문서 변경 이력
================================================================================

2026-01-24 v1.0 - 초기 문서 작성
  - Component-System 아키텍처 정리
  - 컴포넌트별 상세 설명 추가
  - CSS 변수 및 레이아웃 가이드 작성

================================================================================
