# 프로젝트 저장 정책 - 병렬 모델 (Parallel Model) v1.0

> **문서 버전**: 1.0  
> **작성일**: 2025-12-27  
> **상태**: 확정  
> **적용 범위**: Entry, Scratch, Python, AppInventor, Jupyter 모든 플랫폼

---

## 📋 개요

### 배경
기존 시스템에서 플랫폼별로 저장 테이블이 불일치했습니다:
- **Entry**: `ProjectSubmissions`만 사용
- **Scratch**: `UserFiles`만 사용

이로 인해 다음 문제가 발생:
1. Entry 프로젝트는 용량 관리에서 누락
2. Scratch 프로젝트는 학습 평가/진도 관리에서 누락

### 해결책: 병렬 모델
**하나의 파일 저장 시 두 테이블에 동시 기록**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        병렬 모델 (Parallel Model)                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   사용자 저장 요청                                                           │
│         ↓                                                                   │
│   ┌─────────────────┐                                                       │
│   │   S3 업로드      │                                                       │
│   └────────┬────────┘                                                       │
│            ↓                                                                │
│   ┌────────┴────────┐                                                       │
│   ↓                 ↓                                                       │
│ ┌──────────────┐  ┌────────────────────┐                                    │
│ │  UserFiles   │  │ ProjectSubmissions │                                    │
│ │  (용량 관리)  │  │    (학습 평가)      │                                    │
│ ├──────────────┤  ├────────────────────┤                                    │
│ │ 질문:        │  │ 질문:              │                                    │
│ │ "파일이 어디에│  │ "학생이 무엇을     │                                    │
│ │  얼마나?"    │  │  배웠나?"          │                                    │
│ ├──────────────┤  ├────────────────────┤                                    │
│ │ - file_size  │  │ - blocks_count     │                                    │
│ │ - s3_url     │  │ - sprites_count    │                                    │
│ │ - category   │  │ - complexity_score │                                    │
│ │ - user_id    │  │ - submission_type  │                                    │
│ │ - center_id  │  │ - is_shared        │                                    │
│ └──────────────┘  └────────────────────┘                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 핵심 원칙

### 1. 두 테이블의 역할 분리

| 테이블 | 목적 | 핵심 질문 | 연동 시스템 |
|--------|------|-----------|-------------|
| **UserFiles** | 스토리지 용량 관리 | "파일이 어디에, 얼마나?" | quotaChecker, 스토리지 API |
| **ProjectSubmissions** | 학습 평가/진도 관리 | "학생이 무엇을 배웠나?" | CT 분석, 진도관리 대시보드 |

### 2. 갤러리 vs 진도평가 분리

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        갤러리와 진도평가는 일치할 필요 없음!                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   갤러리 (공유)                         진도평가 (분석)                       │
│   ─────────────                         ──────────────                       │
│   ProjectSubmissions                    ProjectSubmissions                  │
│   WHERE is_shared = true                전체 데이터                          │
│                                                                             │
│   ↓                                     ↓                                   │
│   학생이 자랑하고 싶은                  학습 과정의 모든                       │
│   완성작만 표시                         저장본 분석                           │
│                                                                             │
│   예: 10번 저장 중 1개 공유             예: 10번 저장 모두 CT 분석             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3. submission_type 정의

| 값 | 설명 | 용도 |
|----|------|------|
| `autosave` | 자동 저장 (30초/1분 간격) | 작업 손실 방지 |
| `projects` | 수동 저장 (사용자 클릭) | 진행 상태 저장 |
| `submitted` | 최종 제출 | 평가 대상, 과제 제출 |

---

## 🗄️ 데이터베이스 스키마

### UserFiles 테이블 (용량 관리용)

```sql
CREATE TABLE UserFiles (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    center_id INT,
    file_category ENUM('entry', 'scratch', 'python', 'appinventor', 'jupyter', 'gallery', 'board'),
    original_name VARCHAR(255) NOT NULL,
    stored_name VARCHAR(500) NOT NULL,      -- S3 키
    file_size BIGINT NOT NULL,              -- bytes
    file_type VARCHAR(100),                 -- MIME 타입
    s3_url VARCHAR(500),
    related_post_id INT NULL,               -- 게시판 연결용
    is_deleted BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL,
    
    FOREIGN KEY (user_id) REFERENCES Users(id) ON DELETE CASCADE,
    INDEX idx_user_category (user_id, file_category),
    INDEX idx_center (center_id)
);
```

### ProjectSubmissions 테이블 (학습 평가용)

```sql
CREATE TABLE ProjectSubmissions (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    content_map_id INT NULL,                -- 커리큘럼 연결
    
    platform VARCHAR(50) NOT NULL,          -- entry, scratch, python 등
    project_name VARCHAR(255) NOT NULL,
    submission_type ENUM('autosave', 'projects', 'submitted') DEFAULT 'projects',
    
    s3_file_path VARCHAR(500) NOT NULL,
    file_size_kb INT,
    
    -- 자동 분석 결과
    blocks_count INT,
    sprites_count INT,
    variables_count INT,
    functions_count INT,
    complexity_score DECIMAL(5,2),
    
    -- 갤러리 공유
    is_shared BOOLEAN DEFAULT FALSE,
    
    submitted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (user_id) REFERENCES Users(id) ON DELETE CASCADE,
    FOREIGN KEY (content_map_id) REFERENCES ContentMap(id) ON DELETE SET NULL,
    INDEX idx_user_platform (user_id, platform),
    INDEX idx_submission_date (submitted_at),
    INDEX idx_shared (is_shared)
);
```

---

## 🔧 구현 가이드

### 플랫폼별 현재 상태 → 목표 상태

| 플랫폼 | 현재 UserFiles | 현재 ProjectSubmissions | 수정 내용 |
|--------|----------------|-------------------------|-----------|
| Entry | ❌ 미연동 | ✅ 연동 | UserFiles 기록 추가 |
| Scratch | ✅ 연동 | ❌ 미연동 | ProjectSubmissions 기록 추가 |
| Python | ❌ 미구현 | ❌ 미구현 | 양쪽 모두 신규 구현 |
| AppInventor | ❌ 미구현 | ❌ 미구현 | 양쪽 모두 신규 구현 |
| Jupyter | ❌ 미구현 | ❌ 미구현 | 양쪽 모두 신규 구현 |

### 저장 함수 표준 패턴

```javascript
/**
 * 병렬 모델 저장 함수 (모든 플랫폼 공통)
 */
async function saveProjectParallel(userId, centerId, platform, projectData, s3Result) {
    // 1. UserFiles 기록 (용량 관리)
    await recordUserFile(userId, centerId, platform, projectData, s3Result);
    
    // 2. ProjectSubmissions 기록 (학습 평가)
    await recordProjectSubmission(userId, platform, projectData, s3Result);
}

// UserFiles 기록
async function recordUserFile(userId, centerId, platform, projectData, s3Result) {
    const mimeTypes = {
        entry: 'application/x-entry',
        scratch: 'application/x.scratch.sb3',
        python: 'text/x-python',
        appinventor: 'application/x-appinventor',
        jupyter: 'application/x-ipynb+json'
    };
    
    const extensions = {
        entry: '.ent',
        scratch: '.sb3',
        python: '.py',
        appinventor: '.aia',
        jupyter: '.ipynb'
    };
    
    await quotaChecker.recordFile(userId, centerId, {
        file_category: platform,
        original_name: projectData.name + extensions[platform],
        stored_name: s3Result.key,
        file_size: s3Result.size,
        file_type: mimeTypes[platform],
        s3_url: s3Result.url
    });
}

// ProjectSubmissions 기록
async function recordProjectSubmission(userId, platform, projectData, s3Result) {
    const query = `
        INSERT INTO ProjectSubmissions 
        (user_id, platform, project_name, submission_type, s3_file_path, 
         file_size_kb, blocks_count, sprites_count, submitted_at)
        VALUES (?, ?, ?, ?, ?, ?, ?, ?, NOW())
    `;
    
    // 플랫폼별 분석
    const analysis = analyzeProject(platform, projectData);
    
    await db.queryDatabase(query, [
        userId,
        platform,
        projectData.name || 'Untitled',
        projectData.saveType || 'projects',
        s3Result.key,
        Math.ceil(s3Result.size / 1024),
        analysis.blocks || 0,
        analysis.sprites || 0
    ]);
}
```

### 삭제 함수 표준 패턴

```javascript
/**
 * 병렬 모델 삭제 함수
 */
async function deleteProjectParallel(userId, centerId, fileId, platform) {
    // 1. S3에서 파일 삭제
    await s3Manager.deleteProject(s3Key);
    
    // 2. UserFiles에서 삭제 표시
    await quotaChecker.markFileDeleted(fileId);
    
    // 3. ProjectSubmissions 삭제 (또는 soft delete)
    await db.queryDatabase(
        'DELETE FROM ProjectSubmissions WHERE user_id = ? AND s3_file_path = ?',
        [userId, s3Key]
    );
    
    // 4. 용량 반환
    await quotaChecker.decreaseUsage(userId, centerId, fileSize, platform);
}
```

---

## 📊 연동 시스템

### 1. quotaChecker 연동 (UserFiles)

```javascript
// 저장 전 용량 체크
const canSave = await quotaChecker.canUpload(userId, centerId, fileSize);
if (!canSave.allowed) {
    return res.status(413).json({ error: 'QUOTA_EXCEEDED' });
}

// 저장 후 사용량 증가
await quotaChecker.increaseUsage(userId, centerId, fileSize, platform);
```

### 2. CT 분석 연동 (ProjectSubmissions)

```javascript
// 저장 시 자동 분석
const analysis = await projectAnalyzer.analyze(platform, projectData);

// 분석 결과 저장
await db.queryDatabase(`
    UPDATE ProjectSubmissions 
    SET blocks_count = ?, complexity_score = ?
    WHERE id = ?
`, [analysis.blocks, analysis.complexity, submissionId]);
```

### 3. 진도관리 대시보드 연동

```sql
-- 학생별 플랫폼별 저장 횟수
SELECT 
    u.userID,
    ps.platform,
    COUNT(*) as save_count,
    MAX(ps.submitted_at) as last_saved
FROM Users u
JOIN ProjectSubmissions ps ON u.id = ps.user_id
WHERE u.centerID = ?
GROUP BY u.id, ps.platform;
```

### 4. 갤러리 시스템 연동

```sql
-- 공유된 프로젝트 목록
SELECT * FROM ProjectSubmissions 
WHERE is_shared = true 
ORDER BY submitted_at DESC;
```

---

## 📁 관련 파일

### 라우터
- `routes/entryRouter.js` - Entry 저장 (수정 필요)
- `routes/api/scratchRouter.js` - Scratch 저장 (수정 필요)
- `routes/pythonRouter.js` - Python 저장 (신규 구현)
- `routes/appinventorRouter.js` - AppInventor 저장 (신규 구현)
- `routes/api/storageRouter.js` - 스토리지 통합 API

### 라이브러리
- `lib_storage/quotaChecker.js` - 용량 체크 및 UserFiles 기록
- `lib_storage/s3Manager.js` - S3 파일 관리
- `lib_storage/storagePolicy.js` - 정책 정의
- `lib_ct/projectAnalyzer.js` - 프로젝트 분석 (블록 수 등)

---

## 📈 마이그레이션 계획

### Phase 1: Scratch 수정 (1순위)
`scratchRouter.js`에 ProjectSubmissions 기록 추가

### Phase 2: Entry 수정 (2순위)
`entryRouter.js`에 UserFiles 기록 추가

### Phase 3: 기타 플랫폼 구현
Python, AppInventor, Jupyter 순차 구현

### Phase 4: 기존 데이터 동기화 (선택)
```sql
-- UserFiles에만 있는 데이터 → ProjectSubmissions 복사
INSERT INTO ProjectSubmissions (user_id, platform, project_name, s3_file_path, file_size_kb, submitted_at)
SELECT user_id, file_category, original_name, stored_name, CEIL(file_size/1024), created_at
FROM UserFiles 
WHERE file_category IN ('entry', 'scratch')
AND NOT EXISTS (
    SELECT 1 FROM ProjectSubmissions ps 
    WHERE ps.s3_file_path = UserFiles.stored_name
);
```

---

## 📝 변경 이력

| 날짜 | 버전 | 내용 |
|------|------|------|
| 2025-12-27 | 1.0 | 최초 작성 - 병렬 모델 정의 |

---

## 🔗 관련 문서

- [플랫폼_통합저장소_정책명세서.md](./플랫폼_통합저장소_정책명세서.md)
- [S3_스토리지_API_명세서.txt](./S3_스토리지_API_명세서.txt)
- [scratch_저장불러오기_세션관리_명세서.md](./scratch_저장불러오기_세션관리_명세서.md)
- [학습관리) CT기반학습관리시스템구축프로젝트명세서.txt](./학습관리)%20CT기반학습관리시스템구축프로젝트명세서.txt)
