🔥 해설 팝업 기능 구현 작업명세서
📋 프로젝트 개요

프로젝트: educodingnplay 해설 팝업 기능 추가
목적: 문제와 해설을 나란히 볼 수 있는 학습 환경 제공
방식: 플로팅 버튼 + 말풍선 팝업
작성일: 2025년 6월 29일


🏗️ 현재 컴포넌트 시스템 구조
1. 전체 레이아웃
┌─────────────────┬─────────────────┐
│                 │                 │
│   Navigation    │   Content       │ ← 문제 내용 (iframe)
│   Component     │   Component     │
│   (좌측 메뉴)   │                 │
├─────────────────┼─────────────────┤
│                 │   IDE/Quiz      │ ← 코딩/퀴즈 영역
│                 │   Component     │
│                 │                 │
└─────────────────┴─────────────────┘
2. 컴포넌트 시스템 구조
javascript// 기본 컴포넌트 클래스 상속 구조
Component (기본 클래스)
├── NavigationComponent    // 좌측 메뉴
├── ContentComponent      // 문제 표시
├── IDEComponent         // 코드 에디터
├── QuizComponent        // 퀴즈 시스템
└── PPTViewerComponent   // PPT 뷰어
3. 이벤트 시스템
javascript// EventBus 기반 컴포넌트 간 통신
EventBus.publish('menuSelected', data);      // 메뉴 선택
EventBus.publish('problemChanged', data);    // 문제 변경
EventBus.publish('layoutTypeChanged', data); // 레이아웃 변경
4. 데이터 흐름
Google Sheets → getSheetData() → ComponentSystem → 각 컴포넌트
                                      ↓
            EventBus (이벤트 버스) ← → Component 상호작용
5. 파일 구조
educodingnplay/
├── views/template.ejs                    # 메인 템플릿
├── public/js/
│   ├── component-system.js              # 통합 시스템
│   └── components/
│       ├── base/
│       │   ├── Component.js             # 기본 클래스
│       │   ├── EventBus.js              # 이벤트 버스
│       │   └── ComponentFactory.js      # 팩토리
│       ├── nav/NavigationComponent.js   # 네비게이션
│       ├── content/ContentComponent.js  # 콘텐츠
│       ├── ide/IDEComponent.js          # IDE
│       └── quiz/QuizComponent.js        # 퀴즈
└── routes/apiRouter.js                  # API 엔드포인트

🎯 해설 기능 기획 명세
1. 기능 요구사항

트리거: 플로팅 해설 버튼 클릭
위치: ContentComponent 우측하단 (동그란 플로팅 버튼)
팝업: 오른쪽 절반 영역에 말풍선 형태로 표시
내용: 구글시트 F열(해설파일URL)의 MD 파일을 HTML로 렌더링
UX: 문제와 해설을 동시에 볼 수 있도록 배치

2. UI/UX 설계
2-1. 플로팅 버튼
┌─────────────────┬─────────────────┐
│                 │   Content       │
│   Navigation    │   Component     │
│                 │                 │
│                 │            💡   │ ← 플로팅 버튼
├─────────────────┼─────────────────┤
│                 │   IDE/Quiz      │
│                 │   Component     │
└─────────────────┴─────────────────┘
2-2. 팝업 활성화 시
┌─────────────────┬─────────────────┐
│                 │   Content       │
│   Navigation    │   Component     │ ← 문제 그대로 표시
│                 │   (문제 내용)    │
├─────────────────┼─────────────────┤
│                 │ ┌─────────────┐ │
│                 │ │ 💬 해설 팝업 │ │ ← 말풍선 팝업
│                 │ │   [해설내용] │ │   (마크다운→HTML)
│                 │ │       [X]   │ │
│                 │ └─────────────┘ │
└─────────────────┴─────────────────┘
3. 기술 명세

마크다운 렌더링: marked.js (이미 로드됨)
코드 하이라이팅: highlight.js (이미 로드됨)
이벤트 통신: EventBus 패턴
API: RESTful API (/api/get-explanation-md)


📝 상세 작업 명세
TASK 1: ExplanationPopup 컴포넌트 개발
파일: /public/js/components/popup/ExplanationPopup.js (새 파일)
javascriptclass ExplanationPopup extends Component {
  constructor(options = {}) {
    super(options);
    this.visible = false;
    this.currentProblem = null;
  }
  
  // 필수 구현 메서드
  async init() { /* 초기화 */ }
  show(problemData) { /* 팝업 표시 */ }
  hide() { /* 팝업 숨김 */ }
  loadExplanation() { /* 해설 로드 */ }
  renderMarkdown(content) { /* MD→HTML 변환 */ }
  positionPopup() { /* 위치 계산 */ }
}
요구사항:

Component 클래스 상속
EventBus 이벤트 구독: explanation-request
마크다운 렌더링 및 코드 하이라이팅
오른쪽 영역 중앙 배치
드래그 이동 가능 (선택사항)


TASK 2: 플로팅 버튼 추가
파일: /public/js/components/content/ContentComponent.js (기존 파일 수정)
수정 위치 1: initElements() 메서드
javascriptinitElements() {
  // 기존 코드...
  
  // 플로팅 버튼 생성
  this.createFloatingButton();
}

createFloatingButton() {
  const floatingBtn = document.createElement('button');
  floatingBtn.id = 'explanation-floating-btn';
  floatingBtn.className = 'explanation-floating-btn';
  floatingBtn.innerHTML = '<i class="bi bi-lightbulb"></i>';
  floatingBtn.style.display = 'none'; // 초기 숨김
  
  // ContentComponent 컨테이너에 추가
  this.elements.container.appendChild(floatingBtn);
  
  // 클릭 이벤트
  floatingBtn.addEventListener('click', () => {
    this.showExplanationPopup();
  });
}
수정 위치 2: loadProblem() 메서드
javascriptloadProblem(problemNumber) {
  // 기존 로직...
  
  // 해설 파일 존재 여부 확인 후 버튼 표시/숨김
  const hasExplanation = this.checkExplanationExists(problemInfo);
  this.toggleFloatingButton(hasExplanation);
}

TASK 3: CSS 스타일링
파일: /public/css/components/explanation.css (새 파일)
플로팅 버튼 스타일:
css.explanation-floating-btn {
  position: absolute;
  bottom: 20px;
  right: 20px;
  width: 60px;
  height: 60px;
  border-radius: 50%;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border: none;
  color: white;
  font-size: 24px;
  cursor: pointer;
  box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
  z-index: 100;
  transition: all 0.3s ease;
}

.explanation-floating-btn:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 25px rgba(102, 126, 234, 0.6);
}
팝업 스타일:
css.explanation-popup {
  position: fixed;
  background: white;
  border-radius: 15px;
  box-shadow: 0 15px 35px rgba(0,0,0,0.1);
  border: 2px solid #e3f2fd;
  z-index: 1050;
  overflow: hidden;
  
  /* 말풍선 화살표 */
  &::before {
    content: '';
    position: absolute;
    top: -12px;
    left: 40px;
    border-left: 12px solid transparent;
    border-right: 12px solid transparent;
    border-bottom: 12px solid #e3f2fd;
  }
}

TASK 4: API 엔드포인트 개발
파일: /routes/apiRouter.js (기존 파일 수정)
추가 위치: 라인 ~900번 근처
javascript// 해설 파일 가져오기 API
router.get('/get-explanation-md', authenticateUser, async (req, res) => {
  try {
    const { examName, problemNumber } = req.query;
    
    // 입력 검증
    if (!examName || !problemNumber) {
      return res.status(400).json({ 
        success: false, 
        message: '시험명과 문제 번호가 필요합니다.' 
      });
    }
    
    // 문제 데이터에서 해설 URL 찾기 (F열)
    const allProblemData = await getSheetData('problems!A2:L500');
    const formattedProblemNumber = problemNumber.startsWith('p') 
      ? problemNumber 
      : `p${problemNumber.toString().padStart(2, '0')}`;
    
    const problemRow = allProblemData.find(row => 
      row[1]?.toLowerCase() === examName.toLowerCase() && 
      row[2]?.toLowerCase() === formattedProblemNumber.toLowerCase()
    );
    
    if (!problemRow || !problemRow[5]) { // F열 인덱스: 5
      return res.status(404).json({ 
        success: false, 
        message: '해설 파일을 찾을 수 없습니다.' 
      });
    }
    
    // S3에서 MD 파일 가져오기
    const explanationUrl = problemRow[5];
    console.log('해설 파일 URL:', explanationUrl);
    
    const response = await axios.get(explanationUrl);
    
    res.json({
      success: true,
      markdownContent: response.data,
      title: `${examName} - 문제 ${problemNumber} 해설`,
      url: explanationUrl
    });
    
  } catch (error) {
    console.error('해설 파일 로드 오류:', error);
    res.status(500).json({ 
      success: false, 
      message: '해설을 불러오는 중 오류가 발생했습니다.',
      error: error.message
    });
  }
});

TASK 5: 템플릿 업데이트
파일: /views/template.ejs (기존 파일 수정)
추가 위치 1: CSS 링크 섹션
html<!-- 기존 CSS 링크들... -->
<link rel="stylesheet" href="/css/components/explanation.css">
추가 위치 2: 팝업 HTML 구조 (body 내부)
html<!-- 해설 팝업 -->
<div id="explanation-popup" class="explanation-popup" style="display: none;">
  <div class="popup-header">
    <div class="popup-title">
      <i class="bi bi-lightbulb"></i>
      <span id="explanation-title">문제 해설</span>
    </div>
    <button class="popup-close-btn" id="explanation-close-btn">
      <i class="bi bi-x-lg"></i>
    </button>
  </div>
  
  <div class="popup-content">
    <div class="explanation-loading" id="explanation-loading" style="display: none;">
      <div class="loading-spinner"></div>
      <p>해설을 불러오는 중...</p>
    </div>
    
    <div class="explanation-body" id="explanation-body">
      <!-- 마크다운 렌더링된 해설 내용 -->
    </div>
    
    <div class="explanation-error" id="explanation-error" style="display: none;">
      <p class="text-danger">해설을 불러올 수 없습니다.</p>
    </div>
  </div>
</div>
추가 위치 3: 컴포넌트 스크립트 로드
html<!-- 기존 컴포넌트 스크립트들... -->
<script src="/js/components/popup/ExplanationPopup.js"></script>

TASK 6: 컴포넌트 시스템 통합
파일: /public/js/component-system.js (기존 파일 수정)
수정 위치 1: 컴포넌트 등록
javascript// 컴포넌트 등록 부분에 추가
const componentsToRegister = [
  // 기존 컴포넌트들...
  { name: 'explanationPopup', className: 'ExplanationPopup' }
];
수정 위치 2: 컴포넌트 초기화
javascriptasync initializeComponents() {
  // 기존 컴포넌트들...
  
  // ExplanationPopup 초기화
  this.components.explanationPopup = ComponentFactory.create('explanationPopup', {
    elementId: 'explanation-popup'
  });
  
  if (this.components.explanationPopup) {
    await this.components.explanationPopup.init();
  }
}

📊 구글시트 데이터 구조
현재 컬럼 구조 (problems 시트)
A열: 문제파일명 (URL)
B열: 시험지명 (examName)  
C열: 문제번호 (p01, p02...)
D열: 개념파일URL
E열: 정답파일URL 
F열: 해설파일URL ← 이번에 활용
G열: answerType
H열: 정답
I열: 인풋/아웃풋
J열: 난이도
K열: 객관식/주관식
L열: Tags
해설 파일 데이터 흐름
1. 사용자가 플로팅 버튼 클릭
2. ContentComponent에서 현재 문제 정보 확인
3. EventBus로 'explanation-request' 이벤트 발행
4. ExplanationPopup이 이벤트 구독
5. API 호출: /api/get-explanation-md?examName=python&problemNumber=1
6. API에서 problems 시트 F열 조회
7. S3에서 MD 파일 다운로드
8. 마크다운 → HTML 변환 후 팝업에 렌더링

⚡ 구현 우선순위
Phase 1: 핵심 기능

TASK 4: API 엔드포인트 개발 (백엔드)
TASK 1: ExplanationPopup 컴포넌트 개발
TASK 2: ContentComponent에 플로팅 버튼 추가

Phase 2: UI/UX

TASK 3: CSS 스타일링
TASK 5: 템플릿 업데이트

Phase 3: 통합

TASK 6: 컴포넌트 시스템 통합
테스트: 모든 페이지 타입에서 동작 확인


🧪 테스트 계획
단위 테스트

 API 엔드포인트 응답 확인
 마크다운 → HTML 변환 품질
 플로팅 버튼 표시/숨김 로직
 팝업 위치 계산 정확성

통합 테스트

 python, algorithm, certification, aiMath 페이지별 테스트
 문제 변경 시 해설 버튼 상태 업데이트
 팝업과 기존 컴포넌트 간섭 없음 확인
 모바일 반응형 테스트

사용자 시나리오 테스트

 문제 읽기 → 해설 보기 → 문제 다시 보기 흐름
 여러 문제 연속 해설 보기
 IDE/Quiz 컴포넌트와 동시 사용


🚀 배포 체크리스트

 모든 TASK 완료
 CSS 파일 캐시 무효화
 JavaScript 파일 문법 검증
 서버 재시작 후 API 응답 확인
 프로덕션 환경 테스트
 백업 및 롤백 계획 수립


📝 작성자: AI Assistant
📅 작성일: 2025년 6월 29일
🔄 상태: 구현 대기
📋 다음 단계: TASK 4 (API 개발)부터 시작 권장